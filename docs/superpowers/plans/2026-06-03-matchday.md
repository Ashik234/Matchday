# Matchday Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the Matchday FIFA World Cup 2026 single-page companion experience: pitch-themed navbar, Three.js football intro + scroll guide, 7 data-driven sections, three live API integrations with fixture fallback.

**Architecture:** Vite + React 18 + TypeScript SPA. Tailwind CSS keyed to CSS-custom-property tokens. Framer Motion for DOM choreography, Three.js + @react-three/fiber for the persistent football, GSAP ScrollTrigger for scroll pathing. Zustand for ephemeral UI state, TanStack Query for data caching, fixture JSON fallback when APIs fail or rate-limit. All three providers isolated behind adapter files returning normalized domain types.

**Tech Stack:** Vite, React 18, TypeScript (strict), Tailwind CSS, Framer Motion, Three.js + @react-three/fiber + @react-three/drei, GSAP, Zustand, TanStack Query, React Router, lucide-react, flag-icons, react-helmet-async, Anton + Inter + JetBrains Mono via fontsource.

**Specs:** All seven planning docs in `docs/superpowers/specs/2026-06-03-matchday/`. This plan implements them phase-by-phase per `IMPLEMENTATION_ROADMAP.md`.

---

## Phase 0 — Bootstrap

### Task 0.1: Scaffold Vite + React + TypeScript

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.tsx`

- [ ] **Step 1: Scaffold project in current directory**

Run from `e:/ashik/Ashik/Projects/Matchday`:
```bash
pnpm create vite . --template react-ts
```
When prompted that directory is not empty, choose "Ignore files and continue".

- [ ] **Step 2: Install initial dependencies**

```bash
pnpm install
```

- [ ] **Step 3: Verify dev server boots**

```bash
pnpm dev
```
Expected: Vite prints `Local: http://localhost:5173/`. Open in browser → default Vite + React page renders.
Stop with Ctrl+C.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "phase 0: scaffold Vite + React + TypeScript"
```

---

### Task 0.2: Install runtime dependencies

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Install runtime libraries**

```bash
pnpm add react-router-dom @tanstack/react-query zustand framer-motion three @react-three/fiber @react-three/drei gsap lucide-react flag-icons react-helmet-async @fontsource/anton @fontsource-variable/inter @fontsource-variable/jetbrains-mono clsx tailwind-merge
```

- [ ] **Step 2: Install dev libraries**

```bash
pnpm add -D tailwindcss postcss autoprefixer @types/three prettier eslint-config-prettier eslint-plugin-react-hooks
```

- [ ] **Step 3: Verify install clean**

```bash
pnpm install
```
Expected: no errors. `pnpm dev` still serves default page.

- [ ] **Step 4: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "phase 0: install runtime and dev dependencies"
```

---

### Task 0.3: Configure Tailwind

**Files:**
- Create: `tailwind.config.ts`, `postcss.config.js`, `src/styles/globals.css`
- Modify: `src/main.tsx`

- [ ] **Step 1: Init Tailwind**

```bash
pnpm exec tailwindcss init -p
```
This creates `tailwind.config.js` and `postcss.config.js`. Rename `tailwind.config.js` → `tailwind.config.ts`.

- [ ] **Step 2: Replace `tailwind.config.ts` contents**

```ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
};

export default config;
```

- [ ] **Step 3: Create `src/styles/globals.css`**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 4: Import in `src/main.tsx`**

Replace the existing CSS import (likely `./index.css`) with:
```ts
import './styles/globals.css';
```
Delete `src/index.css` and `src/App.css` if present.

- [ ] **Step 5: Verify Tailwind utilities work**

Temporarily edit `src/App.tsx` to put a class on a div: `<div className="bg-red-500 text-white p-4">test</div>`. Run `pnpm dev`, confirm red background. Revert the test markup.

- [ ] **Step 6: Commit**

```bash
git add tailwind.config.ts postcss.config.js src/styles/globals.css src/main.tsx
git rm --cached src/index.css src/App.css 2>/dev/null || true
git commit -m "phase 0: configure Tailwind CSS"
```

---

### Task 0.4: Configure path aliases and TypeScript strict

**Files:**
- Modify: `tsconfig.json`, `tsconfig.app.json`, `vite.config.ts`

- [ ] **Step 1: Add baseUrl + paths to `tsconfig.app.json`**

Inside `compilerOptions`, add:
```json
"baseUrl": ".",
"paths": { "@/*": ["src/*"] },
"strict": true,
"noUncheckedIndexedAccess": true
```

- [ ] **Step 2: Mirror alias in `vite.config.ts`**

Replace contents:
```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'node:path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { '@': path.resolve(__dirname, 'src') },
  },
});
```

- [ ] **Step 3: Verify alias resolves**

Edit `src/App.tsx`: add `import './App.css';` if it was removed → use `import '@/styles/globals.css';` style import elsewhere. Run `pnpm build`. Expected: build succeeds.

- [ ] **Step 4: Commit**

```bash
git add tsconfig.app.json vite.config.ts
git commit -m "phase 0: add @/ path alias and strict TypeScript"
```

---

### Task 0.5: Environment variables scaffold

**Files:**
- Create: `.env.example`
- Modify: `.gitignore`

- [ ] **Step 1: Create `.env.example`**

```
# World Cup 2026 API
VITE_WC2026_KEY=
VITE_WC2026_BASE_URL=https://worldcupjson.net

# API-Football
VITE_API_FOOTBALL_KEY=
VITE_API_FOOTBALL_BASE_URL=https://v3.football.api-sports.io

# BallDontLie FIFA
VITE_BDL_KEY=
VITE_BDL_BASE_URL=https://api.balldontlie.io/fifa/v1

# Force fixture data even when keys are present
VITE_USE_FIXTURES=false
```

- [ ] **Step 2: Confirm `.gitignore` excludes `.env.local`**

`.gitignore` was added during planning. Verify it contains `.env.local`. If not, append:
```
.env
.env.local
.env.*.local
```

- [ ] **Step 3: Commit**

```bash
git add .env.example .gitignore
git commit -m "phase 0: add env scaffold for API keys"
```

---

## Phase 1 — Design System + Shell

### Task 1.1: Design tokens CSS

**Files:**
- Create: `src/styles/tokens.css`
- Modify: `src/styles/globals.css`

- [ ] **Step 1: Create `src/styles/tokens.css`**

```css
:root {
  --c-bg-deep:     #050810;
  --c-bg:          #0A1428;
  --c-bg-elev-1:   #0E1A30;
  --c-bg-elev-2:   #142340;

  --c-pitch:       #1F5A1F;
  --c-pitch-dark:  #0D2818;
  --c-pitch-line:  #FFFFFF;

  --c-gold:        #D4A017;
  --c-gold-light:  #FFD700;

  --c-text:        #F5F7FA;
  --c-text-dim:    #8A95A8;
  --c-text-muted:  #4A5468;

  --c-live:        #FF3B3B;
  --c-success:     #2ECC71;
  --c-loss:        #E74C3C;

  --ease-out:     cubic-bezier(.22,.61,.36,1);
  --ease-impact:  cubic-bezier(.55,.05,.67,.19);
  --ease-spring:  cubic-bezier(.34,1.56,.64,1);
  --dur-fast:     150ms;
  --dur-base:     300ms;
  --dur-slow:     600ms;
  --dur-reveal:   1200ms;

  --shadow-card:   0 4px 16px rgba(0,0,0,0.35);
  --shadow-raised: 0 12px 40px rgba(0,0,0,0.5);
  --shadow-gold:   0 0 24px rgba(212,160,23,0.35);
  --shadow-live:   0 0 16px rgba(255,59,59,0.5);
}

html, body { background: var(--c-bg); color: var(--c-text); }

*:focus-visible {
  outline: 2px solid var(--c-gold);
  outline-offset: 2px;
  border-radius: 4px;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
  }
}
```

- [ ] **Step 2: Import tokens before utilities in `src/styles/globals.css`**

Replace contents:
```css
@import './tokens.css';
@import './fonts.css';

@tailwind base;
@tailwind components;
@tailwind utilities;

html { scroll-behavior: smooth; }
body { font-family: 'Inter Variable', system-ui, sans-serif; -webkit-font-smoothing: antialiased; }
```

- [ ] **Step 3: Commit**

```bash
git add src/styles/tokens.css src/styles/globals.css
git commit -m "phase 1: add design tokens and global styles"
```

---

### Task 1.2: Font setup

**Files:**
- Create: `src/styles/fonts.css`

- [ ] **Step 1: Create `src/styles/fonts.css`**

```css
@import '@fontsource/anton/400.css';
@import '@fontsource-variable/inter/index.css';
@import '@fontsource-variable/jetbrains-mono/index.css';
```

- [ ] **Step 2: Verify dev server loads fonts**

`pnpm dev` → DevTools Network → confirm Anton + Inter + JetBrains Mono woff2 load.

- [ ] **Step 3: Commit**

```bash
git add src/styles/fonts.css
git commit -m "phase 1: load Anton, Inter, JetBrains Mono fonts"
```

---

### Task 1.3: Extend Tailwind theme with tokens

**Files:**
- Modify: `tailwind.config.ts`

- [ ] **Step 1: Replace `tailwind.config.ts`**

```ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        bg: {
          deep: 'var(--c-bg-deep)',
          DEFAULT: 'var(--c-bg)',
          elev1: 'var(--c-bg-elev-1)',
          elev2: 'var(--c-bg-elev-2)',
        },
        pitch: {
          DEFAULT: 'var(--c-pitch)',
          dark: 'var(--c-pitch-dark)',
          line: 'var(--c-pitch-line)',
        },
        gold: {
          DEFAULT: 'var(--c-gold)',
          light: 'var(--c-gold-light)',
        },
        text: {
          DEFAULT: 'var(--c-text)',
          dim: 'var(--c-text-dim)',
          muted: 'var(--c-text-muted)',
        },
        live: 'var(--c-live)',
        success: 'var(--c-success)',
        loss: 'var(--c-loss)',
      },
      fontFamily: {
        display: ['Anton', 'sans-serif'],
        sans: ['"Inter Variable"', 'system-ui', 'sans-serif'],
        mono: ['"JetBrains Mono Variable"', 'ui-monospace', 'monospace'],
      },
      fontSize: {
        display: ['4.5rem', { lineHeight: '1.05', letterSpacing: '-0.01em' }],
        hero: ['6rem', { lineHeight: '1', letterSpacing: '-0.01em' }],
      },
      transitionTimingFunction: {
        'out-quart': 'cubic-bezier(.22,.61,.36,1)',
        impact: 'cubic-bezier(.55,.05,.67,.19)',
        spring: 'cubic-bezier(.34,1.56,.64,1)',
      },
      transitionDuration: {
        fast: '150ms',
        base: '300ms',
        slow: '600ms',
        reveal: '1200ms',
      },
      boxShadow: {
        card: 'var(--shadow-card)',
        raised: 'var(--shadow-raised)',
        gold: 'var(--shadow-gold)',
        live: 'var(--shadow-live)',
      },
      maxWidth: { container: '1440px' },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 2: Smoke test theme**

Edit `src/App.tsx` temporarily:
```tsx
export default function App() {
  return (
    <div className="min-h-screen bg-bg p-12">
      <h1 className="font-display text-display text-text">FIFA 2026</h1>
      <p className="text-gold mt-4">gold accent</p>
      <p className="font-mono text-text-dim">JetBrains Mono · dim</p>
    </div>
  );
}
```
Run `pnpm dev` → confirm Anton headline, gold paragraph, mono dim text on midnight background.

- [ ] **Step 3: Commit**

```bash
git add tailwind.config.ts src/App.tsx
git commit -m "phase 1: extend Tailwind theme with Midnight Pitch tokens"
```

---

### Task 1.4: Class merge utility

**Files:**
- Create: `src/utils/cn.ts`

- [ ] **Step 1: Create `src/utils/cn.ts`**

```ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 2: Commit**

```bash
git add src/utils/cn.ts
git commit -m "phase 1: add cn() class merge utility"
```

---

### Task 1.5: Reduced motion + breakpoint hooks

**Files:**
- Create: `src/hooks/useReducedMotion.ts`, `src/hooks/useBreakpoint.ts`, `src/hooks/useDocumentVisibility.ts`

- [ ] **Step 1: Create `src/hooks/useReducedMotion.ts`**

```ts
import { useEffect, useState } from 'react';

export function useReducedMotion(): boolean {
  const [reduced, setReduced] = useState<boolean>(() =>
    typeof window === 'undefined'
      ? false
      : window.matchMedia('(prefers-reduced-motion: reduce)').matches,
  );

  useEffect(() => {
    const mql = window.matchMedia('(prefers-reduced-motion: reduce)');
    const onChange = (e: MediaQueryListEvent) => setReduced(e.matches);
    mql.addEventListener('change', onChange);
    return () => mql.removeEventListener('change', onChange);
  }, []);

  return reduced;
}
```

- [ ] **Step 2: Create `src/hooks/useBreakpoint.ts`**

```ts
import { useEffect, useState } from 'react';

export type Breakpoint = 'sm' | 'md' | 'lg' | 'xl' | '2xl';

const QUERIES: Record<Breakpoint, string> = {
  sm: '(min-width: 640px)',
  md: '(min-width: 768px)',
  lg: '(min-width: 1024px)',
  xl: '(min-width: 1280px)',
  '2xl': '(min-width: 1536px)',
};

export function useBreakpoint(bp: Breakpoint): boolean {
  const [matches, setMatches] = useState<boolean>(() =>
    typeof window === 'undefined' ? false : window.matchMedia(QUERIES[bp]).matches,
  );

  useEffect(() => {
    const mql = window.matchMedia(QUERIES[bp]);
    const onChange = (e: MediaQueryListEvent) => setMatches(e.matches);
    mql.addEventListener('change', onChange);
    return () => mql.removeEventListener('change', onChange);
  }, [bp]);

  return matches;
}
```

- [ ] **Step 3: Create `src/hooks/useDocumentVisibility.ts`**

```ts
import { useEffect, useState } from 'react';

export function useDocumentVisibility(): boolean {
  const [visible, setVisible] = useState<boolean>(() =>
    typeof document === 'undefined' ? true : document.visibilityState === 'visible',
  );

  useEffect(() => {
    const onChange = () => setVisible(document.visibilityState === 'visible');
    document.addEventListener('visibilitychange', onChange);
    return () => document.removeEventListener('visibilitychange', onChange);
  }, []);

  return visible;
}
```

- [ ] **Step 4: Commit**

```bash
git add src/hooks/
git commit -m "phase 1: add reduced-motion, breakpoint, visibility hooks"
```

---

### Task 1.6: Skip-to-content link

**Files:**
- Create: `src/components/ui/SkipToContent.tsx`

- [ ] **Step 1: Create file**

```tsx
export function SkipToContent() {
  return (
    <a
      href="#main"
      className="sr-only focus:not-sr-only focus:fixed focus:top-2 focus:left-2 focus:z-[100] focus:bg-gold focus:text-bg-deep focus:px-4 focus:py-2 focus:rounded focus:font-semibold"
    >
      Skip to content
    </a>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/SkipToContent.tsx
git commit -m "phase 1: add skip-to-content link"
```

---

### Task 1.7: PitchNavbar shell (static)

**Files:**
- Create: `src/components/navbar/PitchNavbar.tsx`, `src/components/navbar/Logo.tsx`, `src/components/navbar/NavLinks.tsx`, `src/components/navbar/MobileMenu.tsx`

- [ ] **Step 1: Create `src/components/navbar/Logo.tsx`**

```tsx
export function Logo() {
  return (
    <div className="font-display text-2xl tracking-wide text-text leading-none">
      FIFA 2
      <span
        data-ball-anchor="logo"
        className="inline-block w-5 h-5 align-[-2px] mx-0.5 rounded-full bg-gold"
        aria-label="0"
      />
      26
    </div>
  );
}
```

- [ ] **Step 2: Create `src/components/navbar/NavLinks.tsx`**

```tsx
const LINKS = [
  { label: 'Home', href: '#hero' },
  { label: 'Matches', href: '#today-matches' },
  { label: 'Teams', href: '#featured-teams' },
  { label: 'Stadiums', href: '#stadiums' },
  { label: 'Bracket', href: '#road-to-final' },
];

export function NavLinks() {
  return (
    <ul className="flex gap-7">
      {LINKS.map((l) => (
        <li key={l.href}>
          <a
            href={l.href}
            className="text-xs font-semibold tracking-[0.15em] uppercase text-text/85 hover:text-gold transition-colors duration-fast"
          >
            {l.label}
          </a>
        </li>
      ))}
    </ul>
  );
}
```

- [ ] **Step 3: Create `src/components/navbar/MobileMenu.tsx`**

```tsx
import { useState } from 'react';
import { Menu, X } from 'lucide-react';

const LINKS = [
  { label: 'Home', href: '#hero' },
  { label: 'Matches', href: '#today-matches' },
  { label: 'Teams', href: '#featured-teams' },
  { label: 'Stadiums', href: '#stadiums' },
  { label: 'Bracket', href: '#road-to-final' },
];

export function MobileMenu() {
  const [open, setOpen] = useState(false);
  return (
    <>
      <button
        type="button"
        aria-label={open ? 'Close menu' : 'Open menu'}
        aria-expanded={open}
        onClick={() => setOpen((v) => !v)}
        className="p-2 text-text"
      >
        {open ? <X size={22} /> : <Menu size={22} />}
      </button>
      {open && (
        <ul className="fixed inset-x-0 top-[88px] z-50 bg-bg-elev1 border-t border-text-muted/30 p-4 flex flex-col gap-3">
          {LINKS.map((l) => (
            <li key={l.href}>
              <a
                href={l.href}
                onClick={() => setOpen(false)}
                className="block py-2 text-sm font-semibold tracking-[0.15em] uppercase text-text"
              >
                {l.label}
              </a>
            </li>
          ))}
        </ul>
      )}
    </>
  );
}
```

- [ ] **Step 4: Create `src/components/navbar/PitchNavbar.tsx`**

```tsx
import { useBreakpoint } from '@/hooks/useBreakpoint';
import { Logo } from './Logo';
import { NavLinks } from './NavLinks';
import { MobileMenu } from './MobileMenu';

export function PitchNavbar() {
  const isDesktop = useBreakpoint('lg');
  return (
    <header
      data-ball-stage="navbar"
      className="sticky top-0 z-40 w-full"
    >
      <div
        className="relative w-full overflow-hidden"
        style={{
          height: isDesktop ? 140 : 88,
          background:
            'linear-gradient(180deg, var(--c-pitch-dark) 0%, var(--c-pitch) 100%)',
        }}
      >
        {/* mowed stripes */}
        <div
          aria-hidden
          className="absolute inset-0 opacity-30"
          style={{
            backgroundImage:
              'repeating-linear-gradient(90deg, transparent 0 80px, rgba(255,255,255,0.05) 80px 160px)',
          }}
        />
        {/* sideline (ball roll path) */}
        <div
          aria-hidden
          className="absolute left-[4%] right-[4%] bottom-5 h-[3px] bg-pitch-line rounded-full"
        />
        {/* center arc */}
        {isDesktop && (
          <>
            <div
              aria-hidden
              className="absolute left-1/2 bottom-5 -translate-x-1/2 w-[120px] h-[60px] border-[3px] border-pitch-line border-b-0"
              style={{ borderRadius: '120px 120px 0 0' }}
            />
            <div
              aria-hidden
              className="absolute left-1/2 -translate-x-1/2 bottom-[44px] w-[6px] h-[6px] rounded-full bg-pitch-line"
            />
          </>
        )}

        <div className="relative h-full max-w-container mx-auto px-6 lg:px-8 flex items-center justify-between">
          <Logo />
          {isDesktop ? <NavLinks /> : <MobileMenu />}
        </div>
      </div>
    </header>
  );
}
```

- [ ] **Step 5: Wire into `src/App.tsx`**

Replace contents:
```tsx
import { SkipToContent } from '@/components/ui/SkipToContent';
import { PitchNavbar } from '@/components/navbar/PitchNavbar';

export default function App() {
  return (
    <>
      <SkipToContent />
      <PitchNavbar />
      <main id="main" className="min-h-screen">
        <div className="max-w-container mx-auto p-12 text-text-dim">
          Sections will appear here.
        </div>
      </main>
    </>
  );
}
```

- [ ] **Step 6: Visual verify**

`pnpm dev` → desktop: pitch navbar with grass, sideline, arc, penalty spot, logo with gold dot, links right. Resize < 1024px → hamburger appears, no arc.

- [ ] **Step 7: Commit**

```bash
git add src/components/navbar/ src/App.tsx
git commit -m "phase 1: PitchNavbar shell with logo, links, mobile menu"
```

---

### Task 1.8: Footer

**Files:**
- Create: `src/components/ui/Footer.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Create `src/components/ui/Footer.tsx`**

```tsx
export function Footer() {
  return (
    <footer className="border-t border-text-muted/30 bg-bg-deep">
      <div className="max-w-container mx-auto px-6 lg:px-8 py-10 text-text-dim text-sm space-y-3">
        <div className="font-display text-2xl text-text">MATCHDAY</div>
        <p>Unofficial FIFA World Cup 2026 companion.</p>
        <p>
          Data: World Cup 2026 API · API-Football · BallDontLie
        </p>
        <p className="text-xs text-text-muted">Not affiliated with FIFA. © 2026.</p>
      </div>
    </footer>
  );
}
```

- [ ] **Step 2: Add `<Footer>` to `src/App.tsx`**

```tsx
import { Footer } from '@/components/ui/Footer';

// inside the fragment, after </main>:
<Footer />
```

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/Footer.tsx src/App.tsx
git commit -m "phase 1: add footer with credits"
```

---

### Task 1.9: Providers + Router shell

**Files:**
- Create: `src/app/providers.tsx`, `src/app/routes.tsx`
- Modify: `src/main.tsx`, `src/App.tsx`

- [ ] **Step 1: Create `src/app/providers.tsx`**

```tsx
import type { ReactNode } from 'react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { HelmetProvider } from 'react-helmet-async';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 60_000,
      gcTime: 30 * 60_000,
      retry: 2,
      refetchOnWindowFocus: false,
    },
  },
});

export function Providers({ children }: { children: ReactNode }) {
  return (
    <HelmetProvider>
      <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
    </HelmetProvider>
  );
}
```

- [ ] **Step 2: Create `src/app/routes.tsx`**

```tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import App from '@/App';

export function AppRoutes() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<App />} />
      </Routes>
    </BrowserRouter>
  );
}
```

- [ ] **Step 3: Update `src/main.tsx`**

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { Providers } from '@/app/providers';
import { AppRoutes } from '@/app/routes';
import './styles/globals.css';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <Providers>
      <AppRoutes />
    </Providers>
  </StrictMode>,
);
```

- [ ] **Step 4: Verify boot**

`pnpm dev`. Navbar + placeholder + footer still render. No console errors.

- [ ] **Step 5: Commit**

```bash
git add src/app/ src/main.tsx
git commit -m "phase 1: add providers (QueryClient, HelmetProvider) and router shell"
```

---

### Task 1.10: Phase 1 review gate

- [ ] **Step 1: Type-check, lint, build**

```bash
pnpm exec tsc --noEmit
pnpm build
```
Both must succeed cleanly.

- [ ] **Step 2: Commit any fixes**

```bash
git status
# only commit if there are uncommitted fixups
```

---

## Phase 2 — Data Layer

### Task 2.1: Domain types

**Files:**
- Create: `src/data/types/match.ts`, `src/data/types/team.ts`, `src/data/types/group.ts`, `src/data/types/event.ts`, `src/data/types/bracket.ts`, `src/data/types/stadium.ts`, `src/data/types/index.ts`

- [ ] **Step 1: Create `src/data/types/team.ts`**

```ts
export type Federation = 'AFC' | 'CAF' | 'CONCACAF' | 'CONMEBOL' | 'OFC' | 'UEFA';

export type Team = {
  id: string;
  name: string;
  countryCode: string;
  federation: Federation;
  fifaRank?: number;
};
```

- [ ] **Step 2: Create `src/data/types/match.ts`**

```ts
export type MatchStatus = 'scheduled' | 'live' | 'finished' | 'postponed';

export type MatchTeam = {
  teamId: string;
  name: string;
  countryCode: string;
  score?: number;
};

export type Match = {
  id: string;
  status: MatchStatus;
  kickoff: string; // ISO 8601
  minute?: number;
  stage: string;
  group?: string;
  stadium: { name: string; city: string };
  home: MatchTeam;
  away: MatchTeam;
};
```

- [ ] **Step 3: Create `src/data/types/group.ts`**

```ts
import type { Team } from './team';

export type GroupStanding = {
  team: Team;
  played: number;
  won: number;
  drawn: number;
  lost: number;
  gf: number;
  ga: number;
  gd: number;
  pts: number;
};

export type Group = {
  letter: string; // 'A' .. 'L'
  standings: GroupStanding[];
};
```

- [ ] **Step 4: Create `src/data/types/event.ts`**

```ts
export type MatchEventType =
  | 'goal'
  | 'own-goal'
  | 'penalty'
  | 'yellow'
  | 'red'
  | 'sub';

export type MatchEvent = {
  id: string;
  matchId: string;
  minute: number;
  type: MatchEventType;
  teamId: string;
  playerName: string;
  detail?: string;
};
```

- [ ] **Step 5: Create `src/data/types/bracket.ts`**

```ts
export type BracketRound = 'R32' | 'R16' | 'QF' | 'SF' | 'F' | '3rd';

export type BracketSide = {
  teamId: string;
  name: string;
  countryCode: string;
};

export type BracketNode = {
  id: string;
  round: BracketRound;
  matchId?: string;
  home?: BracketSide;
  away?: BracketSide;
  winnerTeamId?: string;
  nextNodeId?: string;
};
```

- [ ] **Step 6: Create `src/data/types/stadium.ts`**

```ts
export type Stadium = {
  id: string;
  name: string;
  city: string;
  country: string;
  capacity: number;
};
```

- [ ] **Step 7: Create barrel `src/data/types/index.ts`**

```ts
export * from './match';
export * from './team';
export * from './group';
export * from './event';
export * from './bracket';
export * from './stadium';
```

- [ ] **Step 8: Commit**

```bash
git add src/data/types/
git commit -m "phase 2: add normalized domain types"
```

---

### Task 2.2: API client + ApiError

**Files:**
- Create: `src/data/api/client.ts`

- [ ] **Step 1: Create file**

```ts
export type ApiSource = 'wc2026' | 'apiFootball' | 'ballDontLie';

export class ApiError extends Error {
  constructor(
    public source: ApiSource,
    public status: number,
    public retryable: boolean,
    message: string,
  ) {
    super(message);
    this.name = 'ApiError';
  }
}

export async function request<T>(
  source: ApiSource,
  url: string,
  init: RequestInit & { signal: AbortSignal },
): Promise<T> {
  let res: Response;
  try {
    res = await fetch(url, init);
  } catch (err) {
    throw new ApiError(source, 0, true, `${source} network error: ${(err as Error).message}`);
  }

  if (!res.ok) {
    const retryable = res.status === 429 || res.status >= 500;
    throw new ApiError(source, res.status, retryable, `${source} ${res.status}`);
  }

  return (await res.json()) as T;
}
```

- [ ] **Step 2: Commit**

```bash
git add src/data/api/client.ts
git commit -m "phase 2: add ApiError and request helper"
```

---

### Task 2.3: Fixture JSON files

**Files:**
- Create: `src/data/fixtures/today-matches.json`, `src/data/fixtures/upcoming-matches.json`, `src/data/fixtures/groups.json`, `src/data/fixtures/live-sample.json`, `src/data/fixtures/teams.json`, `src/data/fixtures/bracket.json`, `src/data/fixtures/final-match.json`, `src/data/fixtures/match-events.json`, `src/data/fixtures/index.ts`

- [ ] **Step 1: Create `src/data/fixtures/teams.json`** (truncated sample — provide 8 teams)

```json
[
  { "id": "USA", "name": "United States", "countryCode": "us", "federation": "CONCACAF", "fifaRank": 13 },
  { "id": "MEX", "name": "Mexico", "countryCode": "mx", "federation": "CONCACAF", "fifaRank": 15 },
  { "id": "CAN", "name": "Canada", "countryCode": "ca", "federation": "CONCACAF", "fifaRank": 41 },
  { "id": "BRA", "name": "Brazil", "countryCode": "br", "federation": "CONMEBOL", "fifaRank": 5 },
  { "id": "ARG", "name": "Argentina", "countryCode": "ar", "federation": "CONMEBOL", "fifaRank": 1 },
  { "id": "FRA", "name": "France", "countryCode": "fr", "federation": "UEFA", "fifaRank": 2 },
  { "id": "ESP", "name": "Spain", "countryCode": "es", "federation": "UEFA", "fifaRank": 3 },
  { "id": "GER", "name": "Germany", "countryCode": "de", "federation": "UEFA", "fifaRank": 16 }
]
```

- [ ] **Step 2: Create `src/data/fixtures/today-matches.json`**

```json
[
  {
    "id": "m-today-1",
    "status": "live",
    "kickoff": "2026-06-03T18:00:00Z",
    "minute": 67,
    "stage": "Group A",
    "group": "A",
    "stadium": { "name": "MetLife Stadium", "city": "East Rutherford" },
    "home": { "teamId": "USA", "name": "United States", "countryCode": "us", "score": 2 },
    "away": { "teamId": "MEX", "name": "Mexico", "countryCode": "mx", "score": 1 }
  },
  {
    "id": "m-today-2",
    "status": "live",
    "kickoff": "2026-06-03T20:30:00Z",
    "minute": 23,
    "stage": "Group D",
    "group": "D",
    "stadium": { "name": "Estadio Azteca", "city": "Mexico City" },
    "home": { "teamId": "BRA", "name": "Brazil", "countryCode": "br", "score": 0 },
    "away": { "teamId": "ARG", "name": "Argentina", "countryCode": "ar", "score": 0 }
  },
  {
    "id": "m-today-3",
    "status": "scheduled",
    "kickoff": "2026-06-03T23:00:00Z",
    "stage": "Group E",
    "group": "E",
    "stadium": { "name": "BMO Field", "city": "Toronto" },
    "home": { "teamId": "FRA", "name": "France", "countryCode": "fr" },
    "away": { "teamId": "GER", "name": "Germany", "countryCode": "de" }
  },
  {
    "id": "m-today-4",
    "status": "scheduled",
    "kickoff": "2026-06-04T01:00:00Z",
    "stage": "Group H",
    "group": "H",
    "stadium": { "name": "SoFi Stadium", "city": "Los Angeles" },
    "home": { "teamId": "ESP", "name": "Spain", "countryCode": "es" },
    "away": { "teamId": "CAN", "name": "Canada", "countryCode": "ca" }
  }
]
```

- [ ] **Step 3: Create `src/data/fixtures/upcoming-matches.json`**

```json
[
  {
    "id": "m-up-1",
    "status": "scheduled",
    "kickoff": "2026-06-04T18:00:00Z",
    "stage": "Group B",
    "group": "B",
    "stadium": { "name": "Lumen Field", "city": "Seattle" },
    "home": { "teamId": "ENG", "name": "England", "countryCode": "gb-eng" },
    "away": { "teamId": "NED", "name": "Netherlands", "countryCode": "nl" }
  },
  {
    "id": "m-up-2",
    "status": "scheduled",
    "kickoff": "2026-06-04T21:00:00Z",
    "stage": "Group C",
    "group": "C",
    "stadium": { "name": "AT&T Stadium", "city": "Arlington" },
    "home": { "teamId": "POR", "name": "Portugal", "countryCode": "pt" },
    "away": { "teamId": "JPN", "name": "Japan", "countryCode": "jp" }
  }
]
```

- [ ] **Step 4: Create `src/data/fixtures/groups.json`** (provide 12 groups A–L; each with 4-team standings — full content)

```json
[
  {
    "letter": "A",
    "standings": [
      { "team": { "id": "USA", "name": "United States", "countryCode": "us", "federation": "CONCACAF", "fifaRank": 13 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 5, "ga": 1, "gd": 4, "pts": 6 },
      { "team": { "id": "MEX", "name": "Mexico", "countryCode": "mx", "federation": "CONCACAF", "fifaRank": 15 }, "played": 2, "won": 1, "drawn": 1, "lost": 0, "gf": 3, "ga": 1, "gd": 2, "pts": 4 },
      { "team": { "id": "AUS", "name": "Australia", "countryCode": "au", "federation": "AFC", "fifaRank": 25 }, "played": 2, "won": 0, "drawn": 1, "lost": 1, "gf": 1, "ga": 3, "gd": -2, "pts": 1 },
      { "team": { "id": "GHA", "name": "Ghana", "countryCode": "gh", "federation": "CAF", "fifaRank": 60 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 0, "ga": 4, "gd": -4, "pts": 0 }
    ]
  },
  {
    "letter": "B",
    "standings": [
      { "team": { "id": "ENG", "name": "England", "countryCode": "gb-eng", "federation": "UEFA", "fifaRank": 4 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 4, "ga": 0, "gd": 4, "pts": 6 },
      { "team": { "id": "NED", "name": "Netherlands", "countryCode": "nl", "federation": "UEFA", "fifaRank": 7 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 3, "ga": 2, "gd": 1, "pts": 3 },
      { "team": { "id": "MAR", "name": "Morocco", "countryCode": "ma", "federation": "CAF", "fifaRank": 14 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 2, "gd": 0, "pts": 3 },
      { "team": { "id": "KOR", "name": "South Korea", "countryCode": "kr", "federation": "AFC", "fifaRank": 22 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 1, "ga": 6, "gd": -5, "pts": 0 }
    ]
  },
  {
    "letter": "C",
    "standings": [
      { "team": { "id": "POR", "name": "Portugal", "countryCode": "pt", "federation": "UEFA", "fifaRank": 6 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 6, "ga": 1, "gd": 5, "pts": 6 },
      { "team": { "id": "JPN", "name": "Japan", "countryCode": "jp", "federation": "AFC", "fifaRank": 17 }, "played": 2, "won": 1, "drawn": 1, "lost": 0, "gf": 4, "ga": 2, "gd": 2, "pts": 4 },
      { "team": { "id": "SUI", "name": "Switzerland", "countryCode": "ch", "federation": "UEFA", "fifaRank": 19 }, "played": 2, "won": 0, "drawn": 1, "lost": 1, "gf": 2, "ga": 4, "gd": -2, "pts": 1 },
      { "team": { "id": "EGY", "name": "Egypt", "countryCode": "eg", "federation": "CAF", "fifaRank": 35 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 1, "ga": 6, "gd": -5, "pts": 0 }
    ]
  },
  {
    "letter": "D",
    "standings": [
      { "team": { "id": "BRA", "name": "Brazil", "countryCode": "br", "federation": "CONMEBOL", "fifaRank": 5 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 5, "ga": 0, "gd": 5, "pts": 6 },
      { "team": { "id": "ARG", "name": "Argentina", "countryCode": "ar", "federation": "CONMEBOL", "fifaRank": 1 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 4, "ga": 2, "gd": 2, "pts": 3 },
      { "team": { "id": "URU", "name": "Uruguay", "countryCode": "uy", "federation": "CONMEBOL", "fifaRank": 11 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 3, "gd": -1, "pts": 3 },
      { "team": { "id": "NZL", "name": "New Zealand", "countryCode": "nz", "federation": "OFC", "fifaRank": 95 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 0, "ga": 6, "gd": -6, "pts": 0 }
    ]
  },
  {
    "letter": "E",
    "standings": [
      { "team": { "id": "FRA", "name": "France", "countryCode": "fr", "federation": "UEFA", "fifaRank": 2 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 4, "ga": 1, "gd": 3, "pts": 6 },
      { "team": { "id": "GER", "name": "Germany", "countryCode": "de", "federation": "UEFA", "fifaRank": 16 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 3, "ga": 2, "gd": 1, "pts": 3 },
      { "team": { "id": "DEN", "name": "Denmark", "countryCode": "dk", "federation": "UEFA", "fifaRank": 20 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 2, "gd": 0, "pts": 3 },
      { "team": { "id": "TUN", "name": "Tunisia", "countryCode": "tn", "federation": "CAF", "fifaRank": 30 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 1, "ga": 5, "gd": -4, "pts": 0 }
    ]
  },
  {
    "letter": "F",
    "standings": [
      { "team": { "id": "BEL", "name": "Belgium", "countryCode": "be", "federation": "UEFA", "fifaRank": 8 }, "played": 2, "won": 1, "drawn": 1, "lost": 0, "gf": 4, "ga": 2, "gd": 2, "pts": 4 },
      { "team": { "id": "CRO", "name": "Croatia", "countryCode": "hr", "federation": "UEFA", "fifaRank": 10 }, "played": 2, "won": 1, "drawn": 1, "lost": 0, "gf": 3, "ga": 1, "gd": 2, "pts": 4 },
      { "team": { "id": "SEN", "name": "Senegal", "countryCode": "sn", "federation": "CAF", "fifaRank": 18 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 2, "gd": 0, "pts": 3 },
      { "team": { "id": "CRC", "name": "Costa Rica", "countryCode": "cr", "federation": "CONCACAF", "fifaRank": 45 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 1, "ga": 5, "gd": -4, "pts": 0 }
    ]
  },
  {
    "letter": "G",
    "standings": [
      { "team": { "id": "ITA", "name": "Italy", "countryCode": "it", "federation": "UEFA", "fifaRank": 9 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 4, "ga": 0, "gd": 4, "pts": 6 },
      { "team": { "id": "COL", "name": "Colombia", "countryCode": "co", "federation": "CONMEBOL", "fifaRank": 12 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 3, "ga": 2, "gd": 1, "pts": 3 },
      { "team": { "id": "NGA", "name": "Nigeria", "countryCode": "ng", "federation": "CAF", "fifaRank": 28 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 3, "gd": -1, "pts": 3 },
      { "team": { "id": "QAT", "name": "Qatar", "countryCode": "qa", "federation": "AFC", "fifaRank": 50 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 0, "ga": 4, "gd": -4, "pts": 0 }
    ]
  },
  {
    "letter": "H",
    "standings": [
      { "team": { "id": "ESP", "name": "Spain", "countryCode": "es", "federation": "UEFA", "fifaRank": 3 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 5, "ga": 1, "gd": 4, "pts": 6 },
      { "team": { "id": "CAN", "name": "Canada", "countryCode": "ca", "federation": "CONCACAF", "fifaRank": 41 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 3, "gd": -1, "pts": 3 },
      { "team": { "id": "POL", "name": "Poland", "countryCode": "pl", "federation": "UEFA", "fifaRank": 26 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 2, "gd": 0, "pts": 3 },
      { "team": { "id": "SAU", "name": "Saudi Arabia", "countryCode": "sa", "federation": "AFC", "fifaRank": 55 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 1, "ga": 4, "gd": -3, "pts": 0 }
    ]
  },
  {
    "letter": "I",
    "standings": [
      { "team": { "id": "SCO", "name": "Scotland", "countryCode": "gb-sct", "federation": "UEFA", "fifaRank": 24 }, "played": 2, "won": 1, "drawn": 1, "lost": 0, "gf": 3, "ga": 1, "gd": 2, "pts": 4 },
      { "team": { "id": "ECU", "name": "Ecuador", "countryCode": "ec", "federation": "CONMEBOL", "fifaRank": 27 }, "played": 2, "won": 1, "drawn": 1, "lost": 0, "gf": 2, "ga": 0, "gd": 2, "pts": 4 },
      { "team": { "id": "AUT", "name": "Austria", "countryCode": "at", "federation": "UEFA", "fifaRank": 23 }, "played": 2, "won": 0, "drawn": 1, "lost": 1, "gf": 1, "ga": 2, "gd": -1, "pts": 1 },
      { "team": { "id": "JAM", "name": "Jamaica", "countryCode": "jm", "federation": "CONCACAF", "fifaRank": 65 }, "played": 2, "won": 0, "drawn": 1, "lost": 1, "gf": 0, "ga": 3, "gd": -3, "pts": 1 }
    ]
  },
  {
    "letter": "J",
    "standings": [
      { "team": { "id": "SUI", "name": "Switzerland", "countryCode": "ch", "federation": "UEFA", "fifaRank": 19 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 5, "ga": 1, "gd": 4, "pts": 6 },
      { "team": { "id": "PER", "name": "Peru", "countryCode": "pe", "federation": "CONMEBOL", "fifaRank": 33 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 3, "ga": 2, "gd": 1, "pts": 3 },
      { "team": { "id": "WAL", "name": "Wales", "countryCode": "gb-wls", "federation": "UEFA", "fifaRank": 38 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 3, "gd": -1, "pts": 3 },
      { "team": { "id": "PAN", "name": "Panama", "countryCode": "pa", "federation": "CONCACAF", "fifaRank": 70 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 1, "ga": 5, "gd": -4, "pts": 0 }
    ]
  },
  {
    "letter": "K",
    "standings": [
      { "team": { "id": "URU", "name": "Uruguay", "countryCode": "uy", "federation": "CONMEBOL", "fifaRank": 11 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 4, "ga": 0, "gd": 4, "pts": 6 },
      { "team": { "id": "TUR", "name": "Turkey", "countryCode": "tr", "federation": "UEFA", "fifaRank": 29 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 3, "ga": 2, "gd": 1, "pts": 3 },
      { "team": { "id": "IRN", "name": "Iran", "countryCode": "ir", "federation": "AFC", "fifaRank": 21 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 2, "gd": 0, "pts": 3 },
      { "team": { "id": "ALG", "name": "Algeria", "countryCode": "dz", "federation": "CAF", "fifaRank": 40 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 0, "ga": 5, "gd": -5, "pts": 0 }
    ]
  },
  {
    "letter": "L",
    "standings": [
      { "team": { "id": "NOR", "name": "Norway", "countryCode": "no", "federation": "UEFA", "fifaRank": 31 }, "played": 2, "won": 2, "drawn": 0, "lost": 0, "gf": 5, "ga": 1, "gd": 4, "pts": 6 },
      { "team": { "id": "CHI", "name": "Chile", "countryCode": "cl", "federation": "CONMEBOL", "fifaRank": 34 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 3, "ga": 2, "gd": 1, "pts": 3 },
      { "team": { "id": "PAR", "name": "Paraguay", "countryCode": "py", "federation": "CONMEBOL", "fifaRank": 48 }, "played": 2, "won": 1, "drawn": 0, "lost": 1, "gf": 2, "ga": 2, "gd": 0, "pts": 3 },
      { "team": { "id": "CMR", "name": "Cameroon", "countryCode": "cm", "federation": "CAF", "fifaRank": 42 }, "played": 2, "won": 0, "drawn": 0, "lost": 2, "gf": 1, "ga": 6, "gd": -5, "pts": 0 }
    ]
  }
]
```

- [ ] **Step 5: Create `src/data/fixtures/live-sample.json`**

```json
{
  "id": "m-today-1",
  "status": "live",
  "kickoff": "2026-06-03T18:00:00Z",
  "minute": 67,
  "stage": "Group A",
  "group": "A",
  "stadium": { "name": "MetLife Stadium", "city": "East Rutherford" },
  "home": { "teamId": "USA", "name": "United States", "countryCode": "us", "score": 2 },
  "away": { "teamId": "MEX", "name": "Mexico", "countryCode": "mx", "score": 1 }
}
```

- [ ] **Step 6: Create `src/data/fixtures/match-events.json`**

```json
[
  { "id": "ev-1", "matchId": "m-today-1", "minute": 12, "type": "goal", "teamId": "USA", "playerName": "C. Pulisic", "detail": "Right footed shot" },
  { "id": "ev-2", "matchId": "m-today-1", "minute": 27, "type": "yellow", "teamId": "MEX", "playerName": "E. Álvarez" },
  { "id": "ev-3", "matchId": "m-today-1", "minute": 38, "type": "goal", "teamId": "MEX", "playerName": "S. Giménez", "detail": "Header" },
  { "id": "ev-4", "matchId": "m-today-1", "minute": 54, "type": "goal", "teamId": "USA", "playerName": "G. Reyna", "detail": "Counter-attack" },
  { "id": "ev-5", "matchId": "m-today-1", "minute": 61, "type": "sub", "teamId": "USA", "playerName": "T. Weah", "detail": "for B. Aaronson" }
]
```

- [ ] **Step 7: Create `src/data/fixtures/bracket.json`** (16-team R16 + QF + SF + F + 3rd, partial filled)

```json
[
  { "id": "n-r16-1", "round": "R16", "matchId": "m-r16-1", "home": { "teamId": "USA", "name": "United States", "countryCode": "us" }, "away": { "teamId": "NED", "name": "Netherlands", "countryCode": "nl" }, "nextNodeId": "n-qf-1" },
  { "id": "n-r16-2", "round": "R16", "matchId": "m-r16-2", "home": { "teamId": "ARG", "name": "Argentina", "countryCode": "ar" }, "away": { "teamId": "POR", "name": "Portugal", "countryCode": "pt" }, "nextNodeId": "n-qf-1" },
  { "id": "n-r16-3", "round": "R16", "matchId": "m-r16-3", "home": { "teamId": "FRA", "name": "France", "countryCode": "fr" }, "away": { "teamId": "JPN", "name": "Japan", "countryCode": "jp" }, "nextNodeId": "n-qf-2" },
  { "id": "n-r16-4", "round": "R16", "matchId": "m-r16-4", "home": { "teamId": "ENG", "name": "England", "countryCode": "gb-eng" }, "away": { "teamId": "GER", "name": "Germany", "countryCode": "de" }, "nextNodeId": "n-qf-2" },
  { "id": "n-r16-5", "round": "R16", "matchId": "m-r16-5", "home": { "teamId": "BRA", "name": "Brazil", "countryCode": "br" }, "away": { "teamId": "BEL", "name": "Belgium", "countryCode": "be" }, "nextNodeId": "n-qf-3" },
  { "id": "n-r16-6", "round": "R16", "matchId": "m-r16-6", "home": { "teamId": "ESP", "name": "Spain", "countryCode": "es" }, "away": { "teamId": "CRO", "name": "Croatia", "countryCode": "hr" }, "nextNodeId": "n-qf-3" },
  { "id": "n-r16-7", "round": "R16", "matchId": "m-r16-7", "home": { "teamId": "ITA", "name": "Italy", "countryCode": "it" }, "away": { "teamId": "URU", "name": "Uruguay", "countryCode": "uy" }, "nextNodeId": "n-qf-4" },
  { "id": "n-r16-8", "round": "R16", "matchId": "m-r16-8", "home": { "teamId": "MEX", "name": "Mexico", "countryCode": "mx" }, "away": { "teamId": "NOR", "name": "Norway", "countryCode": "no" }, "nextNodeId": "n-qf-4" },

  { "id": "n-qf-1", "round": "QF", "nextNodeId": "n-sf-1" },
  { "id": "n-qf-2", "round": "QF", "nextNodeId": "n-sf-1" },
  { "id": "n-qf-3", "round": "QF", "nextNodeId": "n-sf-2" },
  { "id": "n-qf-4", "round": "QF", "nextNodeId": "n-sf-2" },

  { "id": "n-sf-1", "round": "SF", "nextNodeId": "n-f" },
  { "id": "n-sf-2", "round": "SF", "nextNodeId": "n-f" },

  { "id": "n-f", "round": "F" },
  { "id": "n-3rd", "round": "3rd" }
]
```

- [ ] **Step 8: Create `src/data/fixtures/final-match.json`**

```json
{
  "id": "m-final",
  "status": "scheduled",
  "kickoff": "2026-07-19T19:00:00Z",
  "stage": "Final",
  "stadium": { "name": "MetLife Stadium", "city": "East Rutherford" },
  "home": { "teamId": "TBD-H", "name": "TBD", "countryCode": "xx" },
  "away": { "teamId": "TBD-A", "name": "TBD", "countryCode": "xx" }
}
```

- [ ] **Step 9: Create barrel `src/data/fixtures/index.ts`**

```ts
import todayMatches from './today-matches.json';
import upcomingMatches from './upcoming-matches.json';
import groups from './groups.json';
import liveSample from './live-sample.json';
import teams from './teams.json';
import bracket from './bracket.json';
import finalMatch from './final-match.json';
import matchEvents from './match-events.json';

import type { Match, Group, Team, BracketNode, MatchEvent } from '@/data/types';

export const fixtures = {
  todayMatches: todayMatches as Match[],
  upcomingMatches: upcomingMatches as Match[],
  groups: groups as Group[],
  liveSample: liveSample as Match,
  teams: teams as Team[],
  bracket: bracket as BracketNode[],
  finalMatch: finalMatch as Match,
  matchEvents: matchEvents as MatchEvent[],
};
```

- [ ] **Step 10: Enable JSON imports in tsconfig**

In `tsconfig.app.json` `compilerOptions`, ensure `"resolveJsonModule": true` is present. Add `"esModuleInterop": true` if missing.

- [ ] **Step 11: Commit**

```bash
git add src/data/fixtures/ tsconfig.app.json
git commit -m "phase 2: add fixture JSON for all data resources"
```

---

### Task 2.4: WC2026 adapter

**Files:**
- Create: `src/data/api/wc2026.ts`

- [ ] **Step 1: Create file**

```ts
import { request } from './client';
import type { Match, Group, Team, BracketNode, Stadium } from '@/data/types';

const BASE = import.meta.env.VITE_WC2026_BASE_URL ?? 'https://worldcupjson.net';
const KEY = import.meta.env.VITE_WC2026_KEY;

function headers(): HeadersInit {
  return KEY ? { 'X-API-Key': KEY } : {};
}

// Provider response shapes are unknown until phase-2 discovery.
// Adapters return normalized domain types; mappers live here.

export const wc2026 = {
  todayMatches: async (args: { date: string }, signal: AbortSignal): Promise<Match[]> => {
    const raw = await request<unknown>(
      'wc2026',
      `${BASE}/matches/today?date=${encodeURIComponent(args.date)}`,
      { signal, headers: headers() },
    );
    return mapMatches(raw);
  },

  upcomingMatches: async (args: { limit: number }, signal: AbortSignal): Promise<Match[]> => {
    const raw = await request<unknown>(
      'wc2026',
      `${BASE}/matches?status=scheduled&limit=${args.limit}`,
      { signal, headers: headers() },
    );
    return mapMatches(raw);
  },

  liveMatches: async (_: void, signal: AbortSignal): Promise<Match[]> => {
    const raw = await request<unknown>('wc2026', `${BASE}/matches/live`, { signal, headers: headers() });
    return mapMatches(raw);
  },

  match: async (args: { id: string }, signal: AbortSignal): Promise<Match> => {
    const raw = await request<unknown>('wc2026', `${BASE}/matches/${args.id}`, { signal, headers: headers() });
    return mapMatch(raw);
  },

  groups: async (_: void, signal: AbortSignal): Promise<Group[]> => {
    const raw = await request<unknown>('wc2026', `${BASE}/groups`, { signal, headers: headers() });
    return mapGroups(raw);
  },

  teams: async (_: void, signal: AbortSignal): Promise<Team[]> => {
    const raw = await request<unknown>('wc2026', `${BASE}/teams`, { signal, headers: headers() });
    return mapTeams(raw);
  },

  stadiums: async (_: void, signal: AbortSignal): Promise<Stadium[]> => {
    const raw = await request<unknown>('wc2026', `${BASE}/stadiums`, { signal, headers: headers() });
    return mapStadiums(raw);
  },

  bracket: async (_: void, signal: AbortSignal): Promise<BracketNode[]> => {
    const raw = await request<unknown>('wc2026', `${BASE}/bracket`, { signal, headers: headers() });
    return mapBracket(raw);
  },

  finalMatch: async (_: void, signal: AbortSignal): Promise<Match> => {
    const raw = await request<unknown>('wc2026', `${BASE}/matches/final`, { signal, headers: headers() });
    return mapMatch(raw);
  },
};

// Mapper stubs — flesh out once Phase-2 discovery confirms provider shape.
// Until then they pass through if the shape already matches the domain type.
function mapMatches(raw: unknown): Match[] {
  if (!Array.isArray(raw)) return [];
  return raw.map(mapMatch);
}
function mapMatch(raw: unknown): Match {
  return raw as Match;
}
function mapGroups(raw: unknown): Group[] {
  return (raw as Group[]) ?? [];
}
function mapTeams(raw: unknown): Team[] {
  return (raw as Team[]) ?? [];
}
function mapStadiums(raw: unknown): Stadium[] {
  return (raw as Stadium[]) ?? [];
}
function mapBracket(raw: unknown): BracketNode[] {
  return (raw as BracketNode[]) ?? [];
}
```

- [ ] **Step 2: Commit**

```bash
git add src/data/api/wc2026.ts
git commit -m "phase 2: wc2026 adapter with stub mappers"
```

---

### Task 2.5: API-Football adapter

**Files:**
- Create: `src/data/api/apiFootball.ts`

- [ ] **Step 1: Create file**

```ts
import { request } from './client';

const BASE = import.meta.env.VITE_API_FOOTBALL_BASE_URL ?? 'https://v3.football.api-sports.io';
const KEY = import.meta.env.VITE_API_FOOTBALL_KEY;

function headers(): HeadersInit {
  return KEY ? { 'x-apisports-key': KEY } : {};
}

export type H2HSummary = {
  totalMatches: number;
  homeWins: number;
  awayWins: number;
  draws: number;
};

export const apiFootball = {
  h2h: async (
    args: { team1Id: string; team2Id: string },
    signal: AbortSignal,
  ): Promise<H2HSummary> => {
    const raw = await request<unknown>(
      'apiFootball',
      `${BASE}/fixtures/headtohead?h2h=${args.team1Id}-${args.team2Id}`,
      { signal, headers: headers() },
    );
    return mapH2H(raw);
  },
};

function mapH2H(raw: unknown): H2HSummary {
  // Real provider shape returns { response: [...fixtures] } — map once known.
  const arr = (raw as { response?: unknown[] }).response ?? [];
  return { totalMatches: arr.length, homeWins: 0, awayWins: 0, draws: 0 };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/data/api/apiFootball.ts
git commit -m "phase 2: apiFootball adapter with h2h endpoint"
```

---

### Task 2.6: BallDontLie adapter

**Files:**
- Create: `src/data/api/ballDontLie.ts`

- [ ] **Step 1: Create file**

```ts
import { request } from './client';
import type { MatchEvent } from '@/data/types';

const BASE = import.meta.env.VITE_BDL_BASE_URL ?? 'https://api.balldontlie.io/fifa/v1';
const KEY = import.meta.env.VITE_BDL_KEY;

function headers(): HeadersInit {
  return KEY ? { Authorization: `Bearer ${KEY}` } : {};
}

export const ballDontLie = {
  matchEvents: async (args: { matchId: string }, signal: AbortSignal): Promise<MatchEvent[]> => {
    const raw = await request<unknown>(
      'ballDontLie',
      `${BASE}/matches/${args.matchId}/events`,
      { signal, headers: headers() },
    );
    const data = (raw as { data?: unknown[] }).data ?? [];
    return data as MatchEvent[];
  },
};
```

- [ ] **Step 2: Commit**

```bash
git add src/data/api/ballDontLie.ts
git commit -m "phase 2: ballDontLie adapter with matchEvents endpoint"
```

---

### Task 2.7: Enriched query hook helper

**Files:**
- Create: `src/data/queries/useEnriched.ts`

- [ ] **Step 1: Create file**

```ts
import { useQuery, type QueryKey, type UseQueryOptions } from '@tanstack/react-query';

export type EnrichedResult<T> = {
  data: T | undefined;
  isLoading: boolean;
  isError: boolean;
  isFallback: boolean;
  refetch: () => void;
};

export function useEnriched<T>(
  options: Omit<UseQueryOptions<T, Error, T, QueryKey>, 'queryKey'> & {
    queryKey: QueryKey;
    fixture: T;
  },
): EnrichedResult<T> {
  const useFixtures = import.meta.env.VITE_USE_FIXTURES === 'true';
  const q = useQuery<T, Error>({
    ...options,
    queryFn: useFixtures ? () => Promise.resolve(options.fixture) : options.queryFn,
    placeholderData: options.fixture,
  });

  const isFallback = !useFixtures && q.isError && q.data !== undefined;

  return {
    data: q.data,
    isLoading: q.isLoading,
    isError: q.isError && q.data === undefined,
    isFallback,
    refetch: () => {
      void q.refetch();
    },
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/data/queries/useEnriched.ts
git commit -m "phase 2: useEnriched hook with fixture fallback wiring"
```

---

### Task 2.8: TanStack query hooks per resource

**Files:**
- Create: `src/data/queries/useTodayMatches.ts`, `src/data/queries/useUpcomingMatches.ts`, `src/data/queries/useLiveMatches.ts`, `src/data/queries/useGroupStandings.ts`, `src/data/queries/useBracket.ts`, `src/data/queries/useFinalMatch.ts`, `src/data/queries/useMatchEvents.ts`, `src/data/queries/index.ts`

- [ ] **Step 1: Create `src/data/queries/useTodayMatches.ts`**

```ts
import { useEnriched } from './useEnriched';
import { wc2026 } from '@/data/api/wc2026';
import { fixtures } from '@/data/fixtures';

function isoDate(d: Date): string {
  return d.toISOString().slice(0, 10);
}

export function useTodayMatches() {
  const date = isoDate(new Date());
  return useEnriched({
    queryKey: ['today-matches', date],
    queryFn: ({ signal }) => wc2026.todayMatches({ date }, signal!),
    fixture: fixtures.todayMatches,
    staleTime: 60_000,
  });
}
```

- [ ] **Step 2: Create `src/data/queries/useUpcomingMatches.ts`**

```ts
import { useEnriched } from './useEnriched';
import { wc2026 } from '@/data/api/wc2026';
import { fixtures } from '@/data/fixtures';

export function useUpcomingMatches(limit = 2) {
  return useEnriched({
    queryKey: ['upcoming-matches', limit],
    queryFn: ({ signal }) => wc2026.upcomingMatches({ limit }, signal!),
    fixture: fixtures.upcomingMatches.slice(0, limit),
    staleTime: 5 * 60_000,
  });
}
```

- [ ] **Step 3: Create `src/data/queries/useLiveMatches.ts`**

```ts
import { useEnriched } from './useEnriched';
import { wc2026 } from '@/data/api/wc2026';
import { fixtures } from '@/data/fixtures';
import { useDocumentVisibility } from '@/hooks/useDocumentVisibility';

export function useLiveMatches() {
  const visible = useDocumentVisibility();
  return useEnriched({
    queryKey: ['live-matches'],
    queryFn: ({ signal }) => wc2026.liveMatches(undefined, signal!),
    fixture: [fixtures.liveSample],
    staleTime: 15_000,
    refetchInterval: visible ? 15_000 : false,
    refetchIntervalInBackground: false,
  });
}
```

- [ ] **Step 4: Create `src/data/queries/useGroupStandings.ts`**

```ts
import { useEnriched } from './useEnriched';
import { wc2026 } from '@/data/api/wc2026';
import { fixtures } from '@/data/fixtures';

export function useGroupStandings() {
  return useEnriched({
    queryKey: ['groups'],
    queryFn: ({ signal }) => wc2026.groups(undefined, signal!),
    fixture: fixtures.groups,
    staleTime: 5 * 60_000,
  });
}
```

- [ ] **Step 5: Create `src/data/queries/useBracket.ts`**

```ts
import { useEnriched } from './useEnriched';
import { wc2026 } from '@/data/api/wc2026';
import { fixtures } from '@/data/fixtures';

export function useBracket() {
  return useEnriched({
    queryKey: ['bracket'],
    queryFn: ({ signal }) => wc2026.bracket(undefined, signal!),
    fixture: fixtures.bracket,
    staleTime: 5 * 60_000,
  });
}
```

- [ ] **Step 6: Create `src/data/queries/useFinalMatch.ts`**

```ts
import { useEnriched } from './useEnriched';
import { wc2026 } from '@/data/api/wc2026';
import { fixtures } from '@/data/fixtures';

export function useFinalMatch() {
  return useEnriched({
    queryKey: ['final-match'],
    queryFn: ({ signal }) => wc2026.finalMatch(undefined, signal!),
    fixture: fixtures.finalMatch,
    staleTime: 60 * 60_000,
  });
}
```

- [ ] **Step 7: Create `src/data/queries/useMatchEvents.ts`**

```ts
import { useEnriched } from './useEnriched';
import { ballDontLie } from '@/data/api/ballDontLie';
import { fixtures } from '@/data/fixtures';
import { useDocumentVisibility } from '@/hooks/useDocumentVisibility';

export function useMatchEvents(matchId: string | undefined) {
  const visible = useDocumentVisibility();
  return useEnriched({
    queryKey: ['match-events', matchId ?? 'none'],
    queryFn: ({ signal }) => ballDontLie.matchEvents({ matchId: matchId! }, signal!),
    fixture: fixtures.matchEvents,
    enabled: !!matchId,
    staleTime: 15_000,
    refetchInterval: visible && matchId ? 15_000 : false,
  });
}
```

- [ ] **Step 8: Create barrel `src/data/queries/index.ts`**

```ts
export * from './useTodayMatches';
export * from './useUpcomingMatches';
export * from './useLiveMatches';
export * from './useGroupStandings';
export * from './useBracket';
export * from './useFinalMatch';
export * from './useMatchEvents';
```

- [ ] **Step 9: Commit**

```bash
git add src/data/queries/
git commit -m "phase 2: TanStack hooks for all data resources"
```

---

### Task 2.9: FallbackBanner UI component

**Files:**
- Create: `src/components/ui/FallbackBanner.tsx`

- [ ] **Step 1: Create file**

```tsx
import { AlertTriangle } from 'lucide-react';

type Props = {
  message?: string;
  onRetry?: () => void;
};

export function FallbackBanner({ message = 'Showing sample data — live feed unavailable.', onRetry }: Props) {
  return (
    <div className="flex items-center gap-3 mb-4 px-4 py-2 rounded-lg bg-bg-elev1 border border-gold/40 text-text-dim text-sm">
      <AlertTriangle size={16} className="text-gold shrink-0" />
      <span className="flex-1">{message}</span>
      {onRetry && (
        <button
          type="button"
          onClick={onRetry}
          className="text-gold font-semibold uppercase tracking-wider text-xs hover:text-gold-light"
        >
          Retry
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/FallbackBanner.tsx
git commit -m "phase 2: add FallbackBanner component"
```

---

### Task 2.10: Phase 2 smoke test

- [ ] **Step 1: Add temporary smoke probe to `src/App.tsx`**

Inside `<main>`, add:
```tsx
import { useTodayMatches } from '@/data/queries';
// inside component body before return:
const today = useTodayMatches();
console.log('today matches:', today);
```

- [ ] **Step 2: Run dev with fixtures forced**

Add to `.env.local`: `VITE_USE_FIXTURES=true`. Run `pnpm dev`. Open browser console. Expect log with `data: Array(4)`, `isFallback: false`, `isLoading: false`.

- [ ] **Step 3: Remove forced fixtures, leave keys empty**

Set `VITE_USE_FIXTURES=false` (or delete the line). Restart `pnpm dev`. Console should log `data: Array(4)`, `isFallback: true` (queryFn fails for lack of key/CORS, placeholder kicks in).

- [ ] **Step 4: Remove smoke probe**

Revert `src/App.tsx` to its pre-probe state.

- [ ] **Step 5: Type-check + build**

```bash
pnpm exec tsc --noEmit
pnpm build
```

- [ ] **Step 6: Commit any fixups (if any)**

```bash
git status
# commit if needed
```

---

## Phase 3 — Hero Static

### Task 3.1: Zustand UI store

**Files:**
- Create: `src/store/uiStore.ts`

- [ ] **Step 1: Create file**

```ts
import { create } from 'zustand';

export type BallStage =
  | 'pre-intro'
  | 'intro'
  | 'roll-navbar'
  | 'drop-card'
  | 'idle-card'
  | 'scroll-guide'
  | 'parked-countdown';

type AudioState = {
  muted: boolean;
  playing: boolean;
  progress: number;
};

type UIStore = {
  audio: AudioState;
  ballStage: BallStage;
  scrollProgress: number;
  setAudio: (patch: Partial<AudioState>) => void;
  setBallStage: (stage: BallStage) => void;
  setScrollProgress: (p: number) => void;
};

export const useUIStore = create<UIStore>((set) => ({
  audio: { muted: true, playing: false, progress: 0 },
  ballStage: 'pre-intro',
  scrollProgress: 0,
  setAudio: (patch) => set((s) => ({ audio: { ...s.audio, ...patch } })),
  setBallStage: (ballStage) => set({ ballStage }),
  setScrollProgress: (scrollProgress) => set({ scrollProgress }),
}));
```

- [ ] **Step 2: Commit**

```bash
git add src/store/uiStore.ts
git commit -m "phase 3: zustand uiStore for audio, ballStage, scroll progress"
```

---

### Task 3.2: Flag component

**Files:**
- Create: `src/components/ui/Flag.tsx`
- Modify: `src/styles/globals.css`

- [ ] **Step 1: Add flag-icons CSS to `src/styles/globals.css`**

After existing imports:
```css
@import 'flag-icons/css/flag-icons.min.css';
```

- [ ] **Step 2: Create `src/components/ui/Flag.tsx`**

```tsx
import { cn } from '@/utils/cn';

type Size = 'sm' | 'md' | 'lg' | 'xl';

const SIZE_PX: Record<Size, number> = { sm: 20, md: 28, lg: 40, xl: 64 };

type Props = {
  countryCode: string;
  size?: Size;
  rounded?: boolean;
  ariaLabel?: string;
};

export function Flag({ countryCode, size = 'md', rounded = false, ariaLabel }: Props) {
  const px = SIZE_PX[size];
  return (
    <span
      role="img"
      aria-label={ariaLabel ?? countryCode.toUpperCase()}
      className={cn(
        'inline-block bg-cover bg-center shrink-0',
        `fi fi-${countryCode.toLowerCase()}`,
        rounded ? 'rounded-full overflow-hidden' : 'rounded-sm',
      )}
      style={{ width: px, height: Math.round(px * 0.75) }}
    />
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/Flag.tsx src/styles/globals.css
git commit -m "phase 3: add Flag component using flag-icons"
```

---

### Task 3.3: Countdown component

**Files:**
- Create: `src/components/ui/Countdown.tsx`

- [ ] **Step 1: Create file**

```tsx
import { useEffect, useState } from 'react';
import { useDocumentVisibility } from '@/hooks/useDocumentVisibility';

type Format = 'DD:HH:MM:SS' | 'HH:MM:SS' | 'MM:SS';

type Props = {
  to: Date | string;
  format?: Format;
  onComplete?: () => void;
  className?: string;
};

function pad(n: number, len = 2): string {
  return String(Math.max(0, n)).padStart(len, '0');
}

function diff(target: Date) {
  const ms = Math.max(0, target.getTime() - Date.now());
  const total = Math.floor(ms / 1000);
  const days = Math.floor(total / 86_400);
  const hours = Math.floor((total % 86_400) / 3600);
  const minutes = Math.floor((total % 3600) / 60);
  const seconds = total % 60;
  return { days, hours, minutes, seconds, ms };
}

export function Countdown({ to, format = 'HH:MM:SS', onComplete, className }: Props) {
  const target = typeof to === 'string' ? new Date(to) : to;
  const [now, setNow] = useState(() => diff(target));
  const visible = useDocumentVisibility();

  useEffect(() => {
    if (!visible) return;
    const id = window.setInterval(() => {
      const d = diff(target);
      setNow(d);
      if (d.ms === 0) {
        window.clearInterval(id);
        onComplete?.();
      }
    }, 1000);
    return () => window.clearInterval(id);
  }, [target, visible, onComplete]);

  const { days, hours, minutes, seconds } = now;

  let text: string;
  switch (format) {
    case 'DD:HH:MM:SS':
      text = `${pad(days)} : ${pad(hours)} : ${pad(minutes)} : ${pad(seconds)}`;
      break;
    case 'HH:MM:SS':
      text = `${pad(days * 24 + hours)}:${pad(minutes)}:${pad(seconds)}`;
      break;
    case 'MM:SS':
      text = `${pad(minutes)}:${pad(seconds)}`;
      break;
  }

  return (
    <span className={className} aria-live="polite">
      {text}
    </span>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/Countdown.tsx
git commit -m "phase 3: add Countdown component with visibility-gated tick"
```

---

### Task 3.4: LiveDot, Skeleton, basic UI primitives

**Files:**
- Create: `src/components/ui/LiveDot.tsx`, `src/components/ui/Skeleton.tsx`, `src/components/ui/Card.tsx`, `src/components/ui/Pill.tsx`

- [ ] **Step 1: Create `src/components/ui/LiveDot.tsx`**

```tsx
import { cn } from '@/utils/cn';

export function LiveDot({ className }: { className?: string }) {
  return (
    <span className={cn('relative inline-flex h-2 w-2', className)}>
      <span className="absolute inset-0 rounded-full bg-live opacity-80 motion-safe:animate-ping" />
      <span className="relative inline-flex rounded-full h-2 w-2 bg-live" />
    </span>
  );
}
```

- [ ] **Step 2: Create `src/components/ui/Skeleton.tsx`**

```tsx
import { cn } from '@/utils/cn';

export function Skeleton({ className }: { className?: string }) {
  return (
    <div
      className={cn(
        'relative overflow-hidden bg-bg-elev1 rounded-lg',
        'before:absolute before:inset-0 before:-translate-x-full',
        'before:bg-gradient-to-r before:from-transparent before:via-white/5 before:to-transparent',
        'motion-safe:before:animate-[shimmer_1.5s_infinite]',
        className,
      )}
    />
  );
}
```

Add keyframe to `src/styles/globals.css`:
```css
@keyframes shimmer { 100% { transform: translateX(100%); } }
```

- [ ] **Step 3: Create `src/components/ui/Card.tsx`**

```tsx
import type { HTMLAttributes } from 'react';
import { cn } from '@/utils/cn';

type Props = HTMLAttributes<HTMLDivElement> & {
  hover?: boolean;
};

export function Card({ hover = false, className, children, ...rest }: Props) {
  return (
    <div
      className={cn(
        'rounded-xl bg-bg-elev1 border border-white/5 shadow-card p-4 md:p-6 transition-all duration-fast',
        hover && 'hover:-translate-y-1 hover:bg-bg-elev2 hover:shadow-raised',
        className,
      )}
      {...rest}
    >
      {children}
    </div>
  );
}
```

- [ ] **Step 4: Create `src/components/ui/Pill.tsx`**

```tsx
import type { ReactNode } from 'react';
import { cn } from '@/utils/cn';
import { LiveDot } from './LiveDot';

type Variant = 'live' | 'scheduled' | 'final';

const STYLES: Record<Variant, string> = {
  live: 'bg-live text-text',
  scheduled: 'bg-bg-elev2 text-text-dim',
  final: 'bg-text-muted text-bg-deep',
};

export function Pill({ variant, children }: { variant: Variant; children: ReactNode }) {
  return (
    <span
      className={cn(
        'inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-[10px] font-bold uppercase tracking-[0.1em]',
        STYLES[variant],
      )}
    >
      {variant === 'live' && <LiveDot />}
      {children}
    </span>
  );
}
```

- [ ] **Step 5: Commit**

```bash
git add src/components/ui/LiveDot.tsx src/components/ui/Skeleton.tsx src/components/ui/Card.tsx src/components/ui/Pill.tsx src/styles/globals.css
git commit -m "phase 3: add LiveDot, Skeleton, Card, Pill primitives"
```

---

### Task 3.5: formatDate utility

**Files:**
- Create: `src/utils/formatDate.ts`

- [ ] **Step 1: Create file**

```ts
const DAY = ['SUN', 'MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT'];
const MON = ['JAN', 'FEB', 'MAR', 'APR', 'MAY', 'JUN', 'JUL', 'AUG', 'SEP', 'OCT', 'NOV', 'DEC'];

export function formatHeroDate(date: Date): string {
  return `${DAY[date.getDay()]} · ${MON[date.getMonth()]} ${date.getDate()} · ${date.getFullYear()}`;
}

export function formatKickoff(iso: string): string {
  const d = new Date(iso);
  return d.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit' });
}
```

- [ ] **Step 2: Commit**

```bash
git add src/utils/formatDate.ts
git commit -m "phase 3: add date formatting utilities"
```

---

### Task 3.6: MatchCard component (all variants)

**Files:**
- Create: `src/components/ui/MatchCard.tsx`

- [ ] **Step 1: Create file**

```tsx
import { Card } from './Card';
import { Flag } from './Flag';
import { Pill } from './Pill';
import { Countdown } from './Countdown';
import { formatKickoff } from '@/utils/formatDate';
import type { Match } from '@/data/types';

export type MatchCardVariant = 'hero' | 'upcoming' | 'today' | 'live' | 'bracket';

type Props = {
  variant: MatchCardVariant;
  match: Match;
  onClick?: () => void;
};

function statusToPill(match: Match) {
  if (match.status === 'live') return <Pill variant="live">LIVE · {match.minute ?? 0}'</Pill>;
  if (match.status === 'finished') return <Pill variant="final">Final</Pill>;
  return <Pill variant="scheduled">{formatKickoff(match.kickoff)}</Pill>;
}

export function MatchCard({ variant, match, onClick }: Props) {
  const interactive = !!onClick;
  const Wrapper = interactive ? 'button' : 'div';

  if (variant === 'hero') {
    return (
      <Wrapper
        type={interactive ? 'button' : undefined}
        onClick={onClick}
        className="w-full text-left"
      >
        <Card hover={interactive} className="space-y-4">
          <div className="flex items-center justify-between">
            <span className="text-[10px] font-bold uppercase tracking-[0.2em] text-gold">
              {match.stage}
            </span>
            {statusToPill(match)}
          </div>
          <div className="flex items-center justify-between gap-4">
            <TeamSide team={match.home} large />
            <span className="font-mono text-text-dim text-xl">vs</span>
            <TeamSide team={match.away} large align="right" />
          </div>
          <div className="text-xs text-text-dim">
            {match.stadium.name} · {match.stadium.city}
          </div>
        </Card>
      </Wrapper>
    );
  }

  if (variant === 'upcoming' || variant === 'today') {
    return (
      <Wrapper
        type={interactive ? 'button' : undefined}
        onClick={onClick}
        className="w-full text-left"
      >
        <Card hover={interactive}>
          <div className="flex items-center justify-between mb-3">
            <span className="text-[10px] font-bold uppercase tracking-[0.2em] text-gold">
              {match.stage}
            </span>
            {statusToPill(match)}
          </div>
          <div className="flex items-center justify-between gap-3 mb-3">
            <TeamSide team={match.home} />
            <span className="font-mono text-text-dim text-sm">vs</span>
            <TeamSide team={match.away} align="right" />
          </div>
          <div className="flex items-center justify-between text-xs text-text-dim">
            <span>{match.stadium.name}</span>
            {match.status === 'scheduled' && (
              <Countdown
                to={match.kickoff}
                format="HH:MM:SS"
                className="font-mono text-gold"
              />
            )}
          </div>
        </Card>
      </Wrapper>
    );
  }

  if (variant === 'live') {
    return (
      <Card className="bg-bg-elev2 border-live/30 shadow-live">
        <div className="flex items-center justify-between mb-4">
          <Pill variant="live">LIVE · {match.minute ?? 0}'</Pill>
          <span className="text-[10px] font-bold uppercase tracking-[0.2em] text-gold">
            {match.stage}
          </span>
        </div>
        <div className="flex items-center justify-between gap-6">
          <TeamSide team={match.home} large />
          <div className="font-mono text-hero text-text">
            {match.home.score ?? 0} - {match.away.score ?? 0}
          </div>
          <TeamSide team={match.away} large align="right" />
        </div>
      </Card>
    );
  }

  // bracket
  return (
    <Card hover={interactive} className="p-3 text-xs space-y-2">
      <div className="flex items-center justify-between gap-2">
        <TeamSide team={match.home} compact />
        <span className="font-mono text-text-dim">{match.home.score ?? '-'}</span>
      </div>
      <div className="flex items-center justify-between gap-2">
        <TeamSide team={match.away} compact />
        <span className="font-mono text-text-dim">{match.away.score ?? '-'}</span>
      </div>
    </Card>
  );
}

function TeamSide({
  team,
  large = false,
  compact = false,
  align = 'left',
}: {
  team: Match['home'];
  large?: boolean;
  compact?: boolean;
  align?: 'left' | 'right';
}) {
  return (
    <div
      className={
        'flex items-center gap-2 ' + (align === 'right' ? 'flex-row-reverse text-right' : '')
      }
    >
      <Flag countryCode={team.countryCode} size={compact ? 'sm' : large ? 'lg' : 'md'} />
      <span
        className={
          'font-display uppercase tracking-wide ' +
          (compact ? 'text-xs' : large ? 'text-xl' : 'text-base')
        }
      >
        {team.name}
      </span>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/MatchCard.tsx
git commit -m "phase 3: MatchCard with hero/upcoming/today/live/bracket variants"
```

---

### Task 3.7: StadiumBackdrop (CSS bowl + floodlights)

**Files:**
- Create: `src/components/hero/StadiumBackdrop.tsx`

- [ ] **Step 1: Create file**

```tsx
export function StadiumBackdrop() {
  return (
    <div className="absolute inset-0 overflow-hidden">
      <div
        className="absolute inset-0"
        style={{
          background:
            'radial-gradient(ellipse 60% 40% at 50% 100%, rgba(31,90,31,0.5) 0%, transparent 70%),' +
            'radial-gradient(ellipse 80% 50% at 50% 20%, rgba(255,255,255,0.08) 0%, transparent 60%),' +
            'linear-gradient(180deg, #0A1428 0%, #0F1A2E 40%, #0D2818 100%)',
        }}
      />
      <div
        className="absolute left-[10%] right-[10%] top-[8%] h-px"
        style={{
          background: 'linear-gradient(90deg, transparent, rgba(255,255,255,0.6), transparent)',
          filter: 'blur(6px)',
        }}
      />
      <div
        className="absolute left-[-10%] right-[-10%] bottom-[38%] h-[140px]"
        style={{
          background: 'linear-gradient(180deg, #1A2540 0%, #0A1020 100%)',
          borderRadius: '50% 50% 0 0 / 100% 100% 0 0',
          boxShadow: 'inset 0 20px 40px rgba(0,0,0,0.6)',
        }}
      />
      <div
        className="absolute left-0 right-0 bottom-0 h-[38%]"
        style={{
          background: 'linear-gradient(180deg, #1F5A1F 0%, #0D2818 100%)',
        }}
      />
      <div
        aria-hidden
        className="absolute inset-0 motion-safe:animate-[floodPulse_4s_ease-in-out_infinite]"
        style={{
          background:
            'radial-gradient(ellipse 50% 30% at 50% 0%, rgba(255,255,255,0.08), transparent 70%)',
        }}
      />
    </div>
  );
}
```

Add keyframe to `src/styles/globals.css`:
```css
@keyframes floodPulse { 0%,100% { opacity: 0.6; } 50% { opacity: 1; } }
```

- [ ] **Step 2: Commit**

```bash
git add src/components/hero/StadiumBackdrop.tsx src/styles/globals.css
git commit -m "phase 3: StadiumBackdrop with bowl + floodlight pulse"
```

---

### Task 3.8: AudioPlayer

**Files:**
- Create: `src/components/hero/AudioPlayer.tsx`
- Add: `public/anthem.mp3` (placeholder silent or any royalty-free track)

- [ ] **Step 1: Add silent placeholder audio**

Place a short royalty-free MP3 at `public/anthem.mp3`. If none available, generate a 30-second silent file (any tool — for now an empty `anthem.mp3` is acceptable; the player will simply have no audio).

- [ ] **Step 2: Create `src/components/hero/AudioPlayer.tsx`**

```tsx
import { useEffect, useRef, useState } from 'react';
import { Play, Pause, Volume2, VolumeX } from 'lucide-react';
import { useUIStore } from '@/store/uiStore';

const SRC = '/anthem.mp3';

export function AudioPlayer() {
  const audioRef = useRef<HTMLAudioElement | null>(null);
  const { audio, setAudio } = useUIStore();
  const [loaded, setLoaded] = useState(false);

  const ensureLoaded = () => {
    if (loaded) return audioRef.current!;
    const el = new Audio(SRC);
    el.preload = 'auto';
    el.muted = audio.muted;
    el.addEventListener('timeupdate', () => {
      if (el.duration) {
        setAudio({ progress: el.currentTime / el.duration });
      }
    });
    el.addEventListener('ended', () => setAudio({ playing: false, progress: 0 }));
    audioRef.current = el;
    setLoaded(true);
    return el;
  };

  const togglePlay = () => {
    const el = ensureLoaded();
    if (audio.playing) {
      el.pause();
      setAudio({ playing: false });
    } else {
      void el.play();
      setAudio({ playing: true });
    }
  };

  const toggleMute = () => {
    const el = ensureLoaded();
    el.muted = !audio.muted;
    setAudio({ muted: !audio.muted });
  };

  useEffect(() => {
    return () => {
      audioRef.current?.pause();
    };
  }, []);

  return (
    <div className="flex items-center gap-3 p-3 rounded-xl bg-bg-deep/70 border border-white/5 backdrop-blur-sm shadow-card max-w-xs">
      <div className="w-14 h-14 rounded-lg bg-gradient-to-br from-gold to-gold-light shrink-0" aria-hidden />
      <div className="flex-1 min-w-0">
        <div className="text-sm font-semibold text-text truncate">Official Tournament Anthem</div>
        <div className="text-[11px] text-text-dim">FIFA 2026</div>
        <div className="mt-1.5 h-0.5 bg-text-muted/40 rounded-full overflow-hidden">
          <div
            className="h-full bg-gold transition-[width] duration-fast"
            style={{ width: `${audio.progress * 100}%` }}
          />
        </div>
      </div>
      <div className="flex items-center gap-1.5">
        <button
          type="button"
          aria-label={audio.playing ? 'Pause anthem' : 'Play anthem'}
          aria-pressed={audio.playing}
          onClick={togglePlay}
          className="w-9 h-9 rounded-full bg-gold text-bg-deep flex items-center justify-center hover:shadow-gold transition-shadow"
        >
          {audio.playing ? <Pause size={16} /> : <Play size={16} />}
        </button>
        <button
          type="button"
          aria-label={audio.muted ? 'Unmute' : 'Mute'}
          aria-pressed={audio.muted}
          onClick={toggleMute}
          className="w-9 h-9 rounded-full text-text-dim hover:text-text flex items-center justify-center"
        >
          {audio.muted ? <VolumeX size={16} /> : <Volume2 size={16} />}
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add public/anthem.mp3 src/components/hero/AudioPlayer.tsx
git commit -m "phase 3: AudioPlayer with lazy load and mute/play controls"
```

---

### Task 3.9: HeroLeft, HeroCenter, HeroRight, HeroGrid

**Files:**
- Create: `src/components/hero/HeroLeft.tsx`, `src/components/hero/HeroCenter.tsx`, `src/components/hero/HeroRight.tsx`, `src/components/hero/HeroGrid.tsx`, `src/components/hero/TodayMatchesCard.tsx`, `src/components/hero/UpcomingMatchCard.tsx`

- [ ] **Step 1: Create `src/components/hero/HeroLeft.tsx`**

```tsx
import { StadiumBackdrop } from './StadiumBackdrop';
import { AudioPlayer } from './AudioPlayer';

export function HeroLeft() {
  return (
    <div className="relative rounded-2xl overflow-hidden min-h-[480px] flex flex-col justify-end p-6 md:p-8">
      <StadiumBackdrop />
      <div className="relative z-10">
        <div className="text-[10px] font-bold uppercase tracking-[0.3em] text-gold mb-2">
          FIFA WORLD CUP 2026
        </div>
        <h1 className="font-display text-3xl md:text-display text-text leading-[1.05]">
          Experience the journey to the Final.
        </h1>
        <div className="mt-6">
          <AudioPlayer />
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Create `src/components/hero/UpcomingMatchCard.tsx`**

```tsx
import { MatchCard } from '@/components/ui/MatchCard';
import type { Match } from '@/data/types';

export function UpcomingMatchCard({ match }: { match: Match }) {
  return <MatchCard variant="upcoming" match={match} />;
}
```

- [ ] **Step 3: Create `src/components/hero/HeroRight.tsx`**

```tsx
import { useUpcomingMatches } from '@/data/queries';
import { UpcomingMatchCard } from './UpcomingMatchCard';
import { Skeleton } from '@/components/ui/Skeleton';
import { FallbackBanner } from '@/components/ui/FallbackBanner';

export function HeroRight() {
  const { data, isLoading, isFallback, refetch } = useUpcomingMatches(2);

  return (
    <div className="space-y-4">
      <div className="text-[10px] font-bold uppercase tracking-[0.3em] text-gold">
        Up Next
      </div>
      {isFallback && <FallbackBanner onRetry={refetch} />}
      {isLoading && (
        <>
          <Skeleton className="h-40" />
          <Skeleton className="h-40" />
        </>
      )}
      {!isLoading && data?.map((m) => <UpcomingMatchCard key={m.id} match={m} />)}
    </div>
  );
}
```

- [ ] **Step 4: Create `src/components/hero/TodayMatchesCard.tsx`**

```tsx
import { useTodayMatches } from '@/data/queries';
import { MatchCard } from '@/components/ui/MatchCard';
import { Skeleton } from '@/components/ui/Skeleton';
import { FallbackBanner } from '@/components/ui/FallbackBanner';
import { formatHeroDate } from '@/utils/formatDate';

export function TodayMatchesCard() {
  const { data, isLoading, isFallback, refetch } = useTodayMatches();

  const featured = data?.[0];
  const rest = (data ?? []).slice(1);
  const live = (data ?? []).filter((m) => m.status === 'live').length;
  const total = data?.length ?? 0;

  return (
    <div
      data-ball-anchor="date-card"
      className="relative rounded-2xl bg-bg-elev1 border border-white/5 shadow-card p-6 md:p-8 min-h-[480px] flex flex-col"
    >
      <div className="text-[10px] font-bold uppercase tracking-[0.3em] text-gold mb-1">
        Today
      </div>
      <div className="font-display text-2xl md:text-3xl text-text">
        {formatHeroDate(new Date())}
      </div>
      <div className="text-text-dim text-sm mt-1">
        {total} matches today · {live} live now · {Math.max(0, total - live)} upcoming
      </div>

      {isFallback && <div className="mt-4"><FallbackBanner onRetry={refetch} /></div>}

      <div className="mt-6 flex-1 flex flex-col gap-3">
        {isLoading && <Skeleton className="h-48" />}
        {!isLoading && featured && <MatchCard variant="hero" match={featured} />}
        {!isLoading && rest.length > 0 && (
          <div className="grid gap-2">
            {rest.map((m) => (
              <MatchCard key={m.id} variant="today" match={m} />
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Create `src/components/hero/HeroCenter.tsx`**

```tsx
import { TodayMatchesCard } from './TodayMatchesCard';

export function HeroCenter() {
  return <TodayMatchesCard />;
}
```

- [ ] **Step 6: Create `src/components/hero/HeroGrid.tsx`**

```tsx
import { HeroLeft } from './HeroLeft';
import { HeroCenter } from './HeroCenter';
import { HeroRight } from './HeroRight';

export function HeroGrid() {
  return (
    <section
      id="hero"
      data-ball-stage="hero"
      className="max-w-container mx-auto px-4 md:px-8 py-8 md:py-12"
    >
      <div className="grid gap-6 grid-cols-1 lg:grid-cols-[1.1fr_1.4fr_1fr]">
        <div className="order-2 lg:order-1"><HeroLeft /></div>
        <div className="order-1 lg:order-2"><HeroCenter /></div>
        <div className="order-3"><HeroRight /></div>
      </div>
    </section>
  );
}
```

- [ ] **Step 7: Wire into `src/App.tsx`**

Replace the placeholder `<div>` inside `<main>`:
```tsx
import { HeroGrid } from '@/components/hero/HeroGrid';
// ...
<main id="main">
  <HeroGrid />
</main>
```

- [ ] **Step 8: Verify hero**

`pnpm dev`. Confirm: 3-column desktop, mobile stacks center → left → right. Audio buttons clickable, countdowns ticking, fixture data visible.

- [ ] **Step 9: Commit**

```bash
git add src/components/hero/ src/App.tsx
git commit -m "phase 3: hero static (left/center/right + audio player)"
```

---

### Task 3.10: Phase 3 review gate

- [ ] **Step 1: Type-check + build**

```bash
pnpm exec tsc --noEmit
pnpm build
```

- [ ] **Step 2: Manual smoke**

`pnpm dev`, click play (silent OK), confirm mute/unmute, resize across breakpoints. No console errors.

---

## Phase 4 — Three.js Ball + Intro Sequence

### Task 4.1: MotionGate + GlobalCanvas shell

**Files:**
- Create: `src/three/MotionGate.tsx`, `src/three/GlobalCanvas.tsx`, `src/three/Lights.tsx`

- [ ] **Step 1: Create `src/three/MotionGate.tsx`**

```tsx
import type { ReactNode } from 'react';
import { useReducedMotion } from '@/hooks/useReducedMotion';
import { useBreakpoint } from '@/hooks/useBreakpoint';

export function MotionGate({ children }: { children: ReactNode }) {
  const reduced = useReducedMotion();
  const isDesktop = useBreakpoint('lg');
  if (reduced || !isDesktop) return null;
  return <>{children}</>;
}
```

- [ ] **Step 2: Create `src/three/Lights.tsx`**

```tsx
export function Lights() {
  return (
    <>
      <ambientLight intensity={0.5} />
      <directionalLight position={[5, 8, 5]} intensity={1.2} />
    </>
  );
}
```

- [ ] **Step 3: Create `src/three/GlobalCanvas.tsx`**

```tsx
import { Canvas } from '@react-three/fiber';
import { MotionGate } from './MotionGate';
import { Lights } from './Lights';
import { Ball } from './Ball';

export function GlobalCanvas() {
  return (
    <MotionGate>
      <div
        aria-hidden
        className="fixed inset-0 z-30 pointer-events-none"
      >
        <Canvas
          camera={{ position: [0, 0, 10], fov: 35 }}
          gl={{ antialias: true, alpha: true }}
          dpr={[1, 2]}
          frameloop="always"
        >
          <Lights />
          <Ball />
        </Canvas>
      </div>
    </MotionGate>
  );
}
```

- [ ] **Step 4: Commit (Ball file will be added next task — typecheck will fail until then; defer commit)**

Hold commit until Task 4.2 lands.

---

### Task 4.2: Ball mesh (placeholder geometry)

**Files:**
- Create: `src/three/Ball.tsx`

- [ ] **Step 1: Create file**

```tsx
import { useRef } from 'react';
import { useFrame } from '@react-three/fiber';
import type { Mesh } from 'three';
import { useBallController } from './useBallController';

export function Ball() {
  const ref = useRef<Mesh>(null);
  const { position, rotation, scale } = useBallController();

  useFrame(() => {
    if (!ref.current) return;
    ref.current.position.set(position[0], position[1], position[2]);
    ref.current.rotation.set(rotation[0], rotation[1], rotation[2]);
    ref.current.scale.setScalar(scale);
  });

  return (
    <mesh ref={ref}>
      <icosahedronGeometry args={[0.5, 3]} />
      <meshStandardMaterial color="#ffffff" roughness={0.4} metalness={0.1} />
    </mesh>
  );
}
```

- [ ] **Step 2: Note**

PBR textures + Telstar GLB upgrade is a follow-up polish step in Phase 7; placeholder icosahedron ships now.

---

### Task 4.3: useAnchorRect hook

**Files:**
- Create: `src/three/useAnchorRect.ts`

- [ ] **Step 1: Create file**

```ts
import { useEffect, useState } from 'react';

export type AnchorRect = { x: number; y: number; width: number; height: number };

export function useAnchorRect(name: string): AnchorRect | null {
  const [rect, setRect] = useState<AnchorRect | null>(null);

  useEffect(() => {
    const el = document.querySelector<HTMLElement>(`[data-ball-anchor="${name}"]`);
    if (!el) return;

    const update = () => {
      const r = el.getBoundingClientRect();
      setRect({ x: r.x, y: r.y, width: r.width, height: r.height });
    };

    update();
    const ro = new ResizeObserver(update);
    ro.observe(el);
    ro.observe(document.body);
    window.addEventListener('scroll', update, { passive: true });
    window.addEventListener('resize', update);

    return () => {
      ro.disconnect();
      window.removeEventListener('scroll', update);
      window.removeEventListener('resize', update);
    };
  }, [name]);

  return rect;
}
```

---

### Task 4.4: useBallController

**Files:**
- Create: `src/three/useBallController.ts`

- [ ] **Step 1: Create file**

```ts
import { useThree } from '@react-three/fiber';
import { useUIStore } from '@/store/uiStore';
import { useAnchorRect, type AnchorRect } from './useAnchorRect';

export type BallTransform = {
  position: [number, number, number];
  rotation: [number, number, number];
  scale: number;
};

function screenToWorld(
  rect: AnchorRect | null,
  viewport: { width: number; height: number },
  size: { width: number; height: number },
): [number, number, number] {
  if (!rect) return [0, 0, 0];
  const centerX = rect.x + rect.width / 2;
  const centerY = rect.y + rect.height / 2;
  const x = ((centerX / size.width) * 2 - 1) * (viewport.width / 2);
  const y = -((centerY / size.height) * 2 - 1) * (viewport.height / 2);
  return [x, y, 0];
}

export function useBallController(): BallTransform {
  const stage = useUIStore((s) => s.ballStage);
  const scrollProgress = useUIStore((s) => s.scrollProgress);
  const { viewport, size } = useThree();
  const logoRect = useAnchorRect('logo');
  const cardRect = useAnchorRect('date-card');

  const logoWorld = screenToWorld(logoRect, viewport, size);
  const cardWorld = screenToWorld(cardRect, viewport, size);

  switch (stage) {
    case 'pre-intro':
      return { position: logoWorld, rotation: [0, 0, 0], scale: 0 };
    case 'intro':
      return { position: logoWorld, rotation: [0, 0, 0], scale: 1 };
    case 'roll-navbar': {
      // Linear interpolate along x from logo to card target.x.
      const t = Math.min(1, Math.max(0, scrollProgress));
      const x = logoWorld[0] + (cardWorld[0] - logoWorld[0]) * t;
      const rotX = (logoWorld[0] - x) * 4;
      return { position: [x, logoWorld[1], 0], rotation: [rotX, 0, 0], scale: 1 };
    }
    case 'drop-card':
      return { position: cardWorld, rotation: [Math.PI, 0, 0], scale: 1 };
    case 'idle-card':
      return { position: cardWorld, rotation: [Math.PI, performance.now() / 1000, 0], scale: 1 };
    case 'scroll-guide':
      // Curve sampling added in Phase 6.
      return { position: cardWorld, rotation: [Math.PI, performance.now() / 1000, 0], scale: 1 };
    case 'parked-countdown':
      return { position: cardWorld, rotation: [Math.PI, performance.now() / 1000, 0], scale: 1 };
  }
}
```

---

### Task 4.5: Intro timeline orchestration

**Files:**
- Create: `src/three/useIntroTimeline.ts`

- [ ] **Step 1: Create file**

```ts
import { useEffect } from 'react';
import { useUIStore } from '@/store/uiStore';
import { useReducedMotion } from '@/hooks/useReducedMotion';
import { useBreakpoint } from '@/hooks/useBreakpoint';

export function useIntroTimeline() {
  const setBallStage = useUIStore((s) => s.setBallStage);
  const setScrollProgress = useUIStore((s) => s.setScrollProgress);
  const reduced = useReducedMotion();
  const isDesktop = useBreakpoint('lg');

  useEffect(() => {
    if (reduced || !isDesktop) {
      setBallStage('idle-card');
      return;
    }

    let cancelled = false;
    const timers: number[] = [];
    let rafId = 0;

    timers.push(window.setTimeout(() => !cancelled && setBallStage('intro'), 900));
    timers.push(
      window.setTimeout(() => {
        if (cancelled) return;
        setBallStage('roll-navbar');
        const start = performance.now();
        const duration = 1700;
        const tick = (t: number) => {
          if (cancelled) return;
          const p = Math.min(1, (t - start) / duration);
          setScrollProgress(p);
          if (p < 1) rafId = requestAnimationFrame(tick);
          else {
            setBallStage('drop-card');
            timers.push(window.setTimeout(() => !cancelled && setBallStage('idle-card'), 400));
          }
        };
        rafId = requestAnimationFrame(tick);
      }, 1100),
    );

    return () => {
      cancelled = true;
      timers.forEach((id) => window.clearTimeout(id));
      cancelAnimationFrame(rafId);
    };
  }, [reduced, isDesktop, setBallStage, setScrollProgress]);
}
```

---

### Task 4.6: Wire GlobalCanvas + timeline into App

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Add canvas + timeline mount**

```tsx
import { GlobalCanvas } from '@/three/GlobalCanvas';
import { useIntroTimeline } from '@/three/useIntroTimeline';
// ...
export default function App() {
  useIntroTimeline();
  return (
    <>
      <SkipToContent />
      <GlobalCanvas />
      <PitchNavbar />
      <main id="main">
        <HeroGrid />
      </main>
      <Footer />
    </>
  );
}
```

- [ ] **Step 2: Commit batch (4.1 through 4.6)**

```bash
git add src/three/ src/App.tsx
git commit -m "phase 4: GlobalCanvas, Ball, controller, intro timeline"
```

- [ ] **Step 3: Visual verify**

`pnpm dev` desktop ≥ 1024px → ball appears in logo position, rolls to card, settles. Mobile / reduced-motion → no canvas mounts (no console errors).

---

### Task 4.7: Date card impact reveal

**Files:**
- Create: `src/components/hero/DateImpact.tsx`
- Modify: `src/components/hero/TodayMatchesCard.tsx`

- [ ] **Step 1: Create `src/components/hero/DateImpact.tsx`**

```tsx
import { motion, AnimatePresence } from 'framer-motion';
import { useUIStore } from '@/store/uiStore';

export function DateImpact() {
  const stage = useUIStore((s) => s.ballStage);
  const show = stage === 'drop-card' || stage === 'idle-card';
  return (
    <AnimatePresence>
      {show && (
        <motion.div
          key="ripple"
          aria-hidden
          initial={{ scale: 0, opacity: 0.6 }}
          animate={{ scale: 6, opacity: 0 }}
          exit={{ opacity: 0 }}
          transition={{ duration: 0.6, ease: [0.22, 0.61, 0.36, 1] }}
          className="pointer-events-none absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-32 h-32 rounded-full border-2 border-white"
        />
      )}
    </AnimatePresence>
  );
}
```

- [ ] **Step 2: Wrap reveal in `TodayMatchesCard`**

Inside `src/components/hero/TodayMatchesCard.tsx`, wrap the bottom content (the `mt-6 flex-1 flex flex-col gap-3` div) with Framer Motion presence gated by stage. Replace its render:

```tsx
import { motion, AnimatePresence } from 'framer-motion';
import { useUIStore } from '@/store/uiStore';
import { DateImpact } from './DateImpact';

// inside component body, after destructuring data:
const stage = useUIStore((s) => s.ballStage);
const revealed = stage === 'idle-card' || stage === 'scroll-guide' || stage === 'parked-countdown';

// in the JSX, after the stat strip div add <DateImpact />; replace the existing content area with:

<div className="mt-6 flex-1 flex flex-col gap-3 relative">
  <DateImpact />
  <AnimatePresence>
    {revealed && (
      <motion.div
        key="content"
        initial={{ opacity: 0, y: 16 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.4, ease: 'easeOut', staggerChildren: 0.06 }}
        className="flex flex-col gap-3"
      >
        {isLoading && <Skeleton className="h-48" />}
        {!isLoading && featured && <MatchCard variant="hero" match={featured} />}
        {!isLoading && rest.length > 0 && (
          <div className="grid gap-2">
            {rest.map((m) => (
              <MatchCard key={m.id} variant="today" match={m} />
            ))}
          </div>
        )}
      </motion.div>
    )}
  </AnimatePresence>
</div>
```

- [ ] **Step 3: Verify**

`pnpm dev`. Ball drops, ripple expands, content reveals. Reduced-motion: stage starts at `idle-card`, content shows immediately, ripple skipped by CSS reduced-motion override.

- [ ] **Step 4: Commit**

```bash
git add src/components/hero/DateImpact.tsx src/components/hero/TodayMatchesCard.tsx
git commit -m "phase 4: date card impact ripple + stage-gated content reveal"
```

---

### Task 4.8: Phase 4 review gate

- [ ] **Step 1: Type-check + build**

```bash
pnpm exec tsc --noEmit
pnpm build
```

- [ ] **Step 2: Manual perf check**

Open Chrome DevTools Performance. Record load. Confirm ≥ 50fps during ball intro. If lower, note for Phase 7.

---

## Phase 5 — Sections

Each task in this phase: build the section, wire its data hook, fallback banner, skeleton loading, `whileInView` reveal stagger. Each section gets a `<section>` wrapper with `id` and `data-ball-stage` for Phase 6 waypoints.

### Task 5.1: Section primitive

**Files:**
- Create: `src/components/sections/Section.tsx`

- [ ] **Step 1: Create file**

```tsx
import type { ReactNode } from 'react';
import { motion } from 'framer-motion';
import { cn } from '@/utils/cn';

type Props = {
  id: string;
  stage: string;
  eyebrow?: string;
  title: string;
  className?: string;
  children: ReactNode;
};

export function Section({ id, stage, eyebrow, title, className, children }: Props) {
  return (
    <section
      id={id}
      data-ball-stage={stage}
      className={cn('max-w-container mx-auto px-4 md:px-8 py-16 md:py-24', className)}
    >
      <motion.div
        initial={{ opacity: 0, y: 24 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true, amount: 0.3 }}
        transition={{ duration: 0.5, ease: 'easeOut' }}
        className="mb-8"
      >
        {eyebrow && (
          <div className="text-[10px] font-bold uppercase tracking-[0.3em] text-gold mb-2">
            {eyebrow}
          </div>
        )}
        <h2 className="font-display text-3xl md:text-5xl uppercase tracking-tight">{title}</h2>
      </motion.div>
      {children}
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/sections/Section.tsx
git commit -m "phase 5: Section primitive with whileInView reveal"
```

---

### Task 5.2: TodayMatchesSection

**Files:**
- Create: `src/components/sections/TodayMatchesSection.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Create file**

```tsx
import { useState, useMemo } from 'react';
import { motion } from 'framer-motion';
import { Section } from './Section';
import { useTodayMatches } from '@/data/queries';
import { MatchCard } from '@/components/ui/MatchCard';
import { Skeleton } from '@/components/ui/Skeleton';
import { FallbackBanner } from '@/components/ui/FallbackBanner';
import { cn } from '@/utils/cn';

type Filter = 'all' | 'live' | 'upcoming';

export function TodayMatchesSection() {
  const { data, isLoading, isFallback, refetch } = useTodayMatches();
  const [filter, setFilter] = useState<Filter>('all');

  const matches = useMemo(() => {
    const all = data ?? [];
    if (filter === 'live') return all.filter((m) => m.status === 'live');
    if (filter === 'upcoming') return all.filter((m) => m.status === 'scheduled');
    return all;
  }, [data, filter]);

  return (
    <Section
      id="today-matches"
      stage="today-matches"
      eyebrow="Today"
      title="Today's matches"
    >
      {isFallback && <FallbackBanner onRetry={refetch} />}
      <div className="flex gap-2 mb-6">
        {(['all', 'live', 'upcoming'] as Filter[]).map((f) => (
          <button
            key={f}
            type="button"
            onClick={() => setFilter(f)}
            className={cn(
              'px-4 py-1.5 text-xs uppercase tracking-wider rounded-full border transition-colors',
              filter === f
                ? 'bg-gold text-bg-deep border-gold'
                : 'text-text-dim border-text-muted/40 hover:text-text',
            )}
          >
            {f}
          </button>
        ))}
      </div>
      <div className="grid gap-4 grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
        {isLoading &&
          Array.from({ length: 3 }).map((_, i) => <Skeleton key={i} className="h-44" />)}
        {!isLoading &&
          matches.map((m, i) => (
            <motion.div
              key={m.id}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.4, delay: i * 0.08 }}
            >
              <MatchCard variant="today" match={m} />
            </motion.div>
          ))}
      </div>
    </Section>
  );
}
```

- [ ] **Step 2: Add to App**

In `src/App.tsx` `<main>`:
```tsx
import { TodayMatchesSection } from '@/components/sections/TodayMatchesSection';
// ...
<HeroGrid />
<TodayMatchesSection />
```

- [ ] **Step 3: Commit**

```bash
git add src/components/sections/TodayMatchesSection.tsx src/App.tsx
git commit -m "phase 5: TodayMatchesSection with filter chips and stagger"
```

---

### Task 5.3: TournamentProgress

**Files:**
- Create: `src/components/sections/TournamentProgress.tsx`, `src/components/ui/StatTile.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Create `src/components/ui/StatTile.tsx`**

```tsx
import { Card } from './Card';

type Props = { label: string; value: string | number; sub?: string };

export function StatTile({ label, value, sub }: Props) {
  return (
    <Card className="text-center">
      <div className="text-[10px] uppercase tracking-[0.2em] text-gold">{label}</div>
      <div className="font-display text-4xl mt-2">{value}</div>
      {sub && <div className="text-xs text-text-dim mt-1">{sub}</div>}
    </Card>
  );
}
```

- [ ] **Step 2: Create `src/components/sections/TournamentProgress.tsx`**

```tsx
import { useMemo } from 'react';
import { motion } from 'framer-motion';
import { Section } from './Section';
import { StatTile } from '@/components/ui/StatTile';
import { useFinalMatch } from '@/data/queries';

const TOTAL_MATCHES = 104;

export function TournamentProgress() {
  const { data: finalMatch } = useFinalMatch();
  const played = 32; // wire to a real stat hook in a future iteration
  const remaining = TOTAL_MATCHES - played;
  const daysToFinal = useMemo(() => {
    if (!finalMatch) return 0;
    const ms = new Date(finalMatch.kickoff).getTime() - Date.now();
    return Math.max(0, Math.ceil(ms / 86_400_000));
  }, [finalMatch]);

  const pct = Math.round((played / TOTAL_MATCHES) * 100);
  const radius = 70;
  const circumference = 2 * Math.PI * radius;

  return (
    <Section id="progress" stage="progress" eyebrow="Tournament" title="Where we are">
      <div className="grid gap-6 lg:grid-cols-[1fr_auto_1fr] items-center">
        <div className="grid gap-4 sm:grid-cols-2">
          <StatTile label="Current Stage" value="Group" />
          <StatTile label="Played" value={`${played} / ${TOTAL_MATCHES}`} />
        </div>
        <div className="relative w-44 h-44 mx-auto">
          <svg viewBox="0 0 200 200" className="w-full h-full -rotate-90">
            <circle cx="100" cy="100" r={radius} stroke="rgba(255,255,255,0.08)" strokeWidth="10" fill="none" />
            <motion.circle
              cx="100"
              cy="100"
              r={radius}
              stroke="var(--c-gold)"
              strokeWidth="10"
              strokeLinecap="round"
              fill="none"
              strokeDasharray={circumference}
              initial={{ strokeDashoffset: circumference }}
              whileInView={{ strokeDashoffset: circumference * (1 - played / TOTAL_MATCHES) }}
              viewport={{ once: true }}
              transition={{ duration: 1.2, ease: 'easeOut' }}
            />
          </svg>
          <div className="absolute inset-0 flex flex-col items-center justify-center">
            <div className="font-display text-4xl">{pct}%</div>
            <div className="text-[10px] uppercase tracking-[0.2em] text-text-dim">Played</div>
          </div>
        </div>
        <div className="grid gap-4 sm:grid-cols-2">
          <StatTile label="Remaining" value={remaining} />
          <StatTile label="Days to Final" value={daysToFinal} />
        </div>
      </div>
    </Section>
  );
}
```

- [ ] **Step 3: Mount**

In `src/App.tsx`:
```tsx
import { TournamentProgress } from '@/components/sections/TournamentProgress';
// ...
<TodayMatchesSection />
<TournamentProgress />
```

- [ ] **Step 4: Commit**

```bash
git add src/components/sections/TournamentProgress.tsx src/components/ui/StatTile.tsx src/App.tsx
git commit -m "phase 5: TournamentProgress with radial ring + stat tiles"
```

---

### Task 5.4: GroupStandings

**Files:**
- Create: `src/components/sections/GroupStandings.tsx`, `src/components/sections/GroupCard.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Create `src/components/sections/GroupCard.tsx`**

```tsx
import { motion } from 'framer-motion';
import { Card } from '@/components/ui/Card';
import { Flag } from '@/components/ui/Flag';
import type { Group } from '@/data/types';

export function GroupCard({ group }: { group: Group }) {
  return (
    <Card>
      <div className="text-[10px] uppercase tracking-[0.3em] text-gold mb-3">Group {group.letter}</div>
      <table className="w-full text-xs">
        <thead className="text-text-dim">
          <tr>
            <th className="text-left pb-2">Team</th>
            <th className="pb-2">P</th>
            <th className="pb-2">W</th>
            <th className="pb-2">D</th>
            <th className="pb-2">L</th>
            <th className="pb-2">GD</th>
            <th className="pb-2 text-right">Pts</th>
          </tr>
        </thead>
        <tbody>
          {group.standings.map((row, i) => (
            <motion.tr
              key={row.team.id}
              initial={{ opacity: 0, x: -16 }}
              whileInView={{ opacity: 1, x: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.3, delay: i * 0.05 }}
              className={i < 2 ? 'border-l-2 border-gold pl-1' : ''}
            >
              <td className="py-1.5 flex items-center gap-2">
                <Flag countryCode={row.team.countryCode} size="sm" />
                <span className="font-semibold">{row.team.name}</span>
              </td>
              <td className="text-center">{row.played}</td>
              <td className="text-center">{row.won}</td>
              <td className="text-center">{row.drawn}</td>
              <td className="text-center">{row.lost}</td>
              <td className="text-center font-mono">{row.gd >= 0 ? `+${row.gd}` : row.gd}</td>
              <td className="text-right font-mono font-bold">{row.pts}</td>
            </motion.tr>
          ))}
        </tbody>
      </table>
    </Card>
  );
}
```

- [ ] **Step 2: Create `src/components/sections/GroupStandings.tsx`**

```tsx
import { motion } from 'framer-motion';
import { Section } from './Section';
import { GroupCard } from './GroupCard';
import { useGroupStandings } from '@/data/queries';
import { Skeleton } from '@/components/ui/Skeleton';
import { FallbackBanner } from '@/components/ui/FallbackBanner';

export function GroupStandings() {
  const { data, isLoading, isFallback, refetch } = useGroupStandings();

  return (
    <Section id="groups" stage="groups" eyebrow="Groups" title="Group Standings">
      {isFallback && <FallbackBanner onRetry={refetch} />}
      <div className="grid gap-4 grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
        {isLoading &&
          Array.from({ length: 6 }).map((_, i) => <Skeleton key={i} className="h-56" />)}
        {!isLoading &&
          data?.map((g, i) => (
            <motion.div
              key={g.letter}
              initial={{ opacity: 0, y: 16 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.4, delay: i * 0.05 }}
            >
              <GroupCard group={g} />
            </motion.div>
          ))}
      </div>
    </Section>
  );
}
```

- [ ] **Step 3: Mount**

```tsx
import { GroupStandings } from '@/components/sections/GroupStandings';
// ...
<TournamentProgress />
<GroupStandings />
```

- [ ] **Step 4: Commit**

```bash
git add src/components/sections/GroupCard.tsx src/components/sections/GroupStandings.tsx src/App.tsx
git commit -m "phase 5: GroupStandings with 12-group grid + cascading rows"
```

---

### Task 5.5: FeaturedTeams

**Files:**
- Create: `src/components/sections/FeaturedTeams.tsx`, `src/components/sections/TeamCarouselCard.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Create `src/components/sections/TeamCarouselCard.tsx`**

```tsx
import { Card } from '@/components/ui/Card';
import { Flag } from '@/components/ui/Flag';
import type { Team } from '@/data/types';

export function TeamCarouselCard({ team }: { team: Team }) {
  return (
    <Card hover className="min-w-[260px] snap-start">
      <div className="flex items-center gap-3 mb-3">
        <Flag countryCode={team.countryCode} size="lg" />
        <div>
          <div className="font-display text-xl">{team.name}</div>
          <div className="text-[10px] uppercase tracking-[0.2em] text-text-dim">{team.federation}</div>
        </div>
      </div>
      <div className="flex items-center justify-between text-xs">
        <span className="text-text-dim">FIFA Rank</span>
        <span className="font-mono text-gold">#{team.fifaRank ?? '—'}</span>
      </div>
    </Card>
  );
}
```

- [ ] **Step 2: Create `src/components/sections/FeaturedTeams.tsx`**

```tsx
import { Section } from './Section';
import { TeamCarouselCard } from './TeamCarouselCard';
import { fixtures } from '@/data/fixtures';

export function FeaturedTeams() {
  // Use the static teams fixture for the carousel — featured-team selection
  // logic (followers/popularity) is post-MVP.
  const teams = fixtures.teams;
  return (
    <Section id="featured-teams" stage="featured-teams" eyebrow="Featured" title="Teams to watch">
      <div className="flex gap-4 overflow-x-auto snap-x snap-mandatory pb-4 -mx-4 px-4">
        {teams.map((t) => (
          <TeamCarouselCard key={t.id} team={t} />
        ))}
      </div>
    </Section>
  );
}
```

- [ ] **Step 3: Mount**

```tsx
import { FeaturedTeams } from '@/components/sections/FeaturedTeams';
// ...
<GroupStandings />
<FeaturedTeams />
```

- [ ] **Step 4: Commit**

```bash
git add src/components/sections/FeaturedTeams.tsx src/components/sections/TeamCarouselCard.tsx src/App.tsx
git commit -m "phase 5: FeaturedTeams horizontal scroll carousel"
```

---

### Task 5.6: LiveMatchCenter

**Files:**
- Create: `src/components/sections/LiveMatchCenter.tsx`, `src/components/sections/EventTimeline.tsx`, `src/components/sections/StatBars.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Create `src/components/sections/EventTimeline.tsx`**

```tsx
import { motion } from 'framer-motion';
import type { MatchEvent } from '@/data/types';

const ICON: Record<MatchEvent['type'], string> = {
  goal: '⚽',
  'own-goal': '🙃',
  penalty: '⚽',
  yellow: '🟨',
  red: '🟥',
  sub: '↔︎',
};

export function EventTimeline({ events }: { events: MatchEvent[] }) {
  return (
    <ul className="space-y-2">
      {events.map((e) => (
        <motion.li
          key={e.id}
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.3 }}
          className="flex items-center gap-3 text-sm"
        >
          <span className="w-10 font-mono text-text-dim">{e.minute}'</span>
          <span aria-hidden>{ICON[e.type]}</span>
          <span className="font-semibold">{e.playerName}</span>
          {e.detail && <span className="text-text-dim text-xs">— {e.detail}</span>}
        </motion.li>
      ))}
    </ul>
  );
}
```

- [ ] **Step 2: Create `src/components/sections/StatBars.tsx`**

```tsx
import { motion } from 'framer-motion';

type Stat = { label: string; home: number; away: number; unit?: string };

const STATS: Stat[] = [
  { label: 'Possession', home: 58, away: 42, unit: '%' },
  { label: 'Shots', home: 14, away: 9 },
  { label: 'On Target', home: 6, away: 3 },
  { label: 'Corners', home: 5, away: 2 },
];

export function StatBars() {
  return (
    <div className="space-y-3">
      {STATS.map((s) => {
        const total = s.home + s.away || 1;
        return (
          <div key={s.label}>
            <div className="flex justify-between text-xs text-text-dim mb-1">
              <span className="font-mono">{s.home}{s.unit ?? ''}</span>
              <span className="uppercase tracking-wider">{s.label}</span>
              <span className="font-mono">{s.away}{s.unit ?? ''}</span>
            </div>
            <div className="flex h-1.5 rounded-full overflow-hidden bg-bg-elev1">
              <motion.div
                className="bg-gold"
                initial={{ width: 0 }}
                whileInView={{ width: `${(s.home / total) * 100}%` }}
                viewport={{ once: true }}
                transition={{ duration: 0.8 }}
              />
              <motion.div
                className="bg-text-muted"
                initial={{ width: 0 }}
                whileInView={{ width: `${(s.away / total) * 100}%` }}
                viewport={{ once: true }}
                transition={{ duration: 0.8 }}
              />
            </div>
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 3: Create `src/components/sections/LiveMatchCenter.tsx`**

```tsx
import { Section } from './Section';
import { MatchCard } from '@/components/ui/MatchCard';
import { useLiveMatches, useMatchEvents } from '@/data/queries';
import { Skeleton } from '@/components/ui/Skeleton';
import { FallbackBanner } from '@/components/ui/FallbackBanner';
import { EventTimeline } from './EventTimeline';
import { StatBars } from './StatBars';

export function LiveMatchCenter() {
  const { data, isLoading, isFallback, refetch } = useLiveMatches();
  const liveMatch = data?.[0];
  const { data: events } = useMatchEvents(liveMatch?.id);

  return (
    <Section id="live" stage="live" eyebrow="Live" title="Live Match Center">
      {isFallback && <FallbackBanner onRetry={refetch} />}
      {isLoading && <Skeleton className="h-72" />}
      {!isLoading && !liveMatch && (
        <div className="text-center py-12 text-text-dim">
          No live matches right now. Check back at the next kickoff.
        </div>
      )}
      {!isLoading && liveMatch && (
        <div className="grid gap-6 lg:grid-cols-[1.4fr_1fr]">
          <div className="space-y-6">
            <MatchCard variant="live" match={liveMatch} />
            <StatBars />
          </div>
          <div className="rounded-xl bg-bg-elev1 border border-white/5 p-4 max-h-96 overflow-y-auto">
            <div className="text-[10px] uppercase tracking-[0.3em] text-gold mb-3">Timeline</div>
            <EventTimeline events={events ?? []} />
          </div>
        </div>
      )}
    </Section>
  );
}
```

- [ ] **Step 4: Mount**

```tsx
import { LiveMatchCenter } from '@/components/sections/LiveMatchCenter';
// ...
<FeaturedTeams />
<LiveMatchCenter />
```

- [ ] **Step 5: Commit**

```bash
git add src/components/sections/LiveMatchCenter.tsx src/components/sections/EventTimeline.tsx src/components/sections/StatBars.tsx src/App.tsx
git commit -m "phase 5: LiveMatchCenter with timeline + stat bars"
```

---

### Task 5.7: RoadToFinal

**Files:**
- Create: `src/components/sections/RoadToFinal.tsx`, `src/components/sections/BracketColumn.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Create `src/components/sections/BracketColumn.tsx`**

```tsx
import { motion } from 'framer-motion';
import { Flag } from '@/components/ui/Flag';
import type { BracketNode } from '@/data/types';

export function BracketColumn({
  label,
  nodes,
  delay,
}: {
  label: string;
  nodes: BracketNode[];
  delay: number;
}) {
  return (
    <div className="flex flex-col gap-4 min-w-[180px]">
      <div className="text-[10px] uppercase tracking-[0.3em] text-gold">{label}</div>
      {nodes.map((n, i) => (
        <motion.div
          key={n.id}
          initial={{ opacity: 0, x: -20 }}
          whileInView={{ opacity: 1, x: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.4, delay: delay + i * 0.08 }}
          className="rounded-lg bg-bg-elev1 border border-white/5 p-3 text-xs space-y-2"
        >
          {n.home ? (
            <div className="flex items-center gap-2">
              <Flag countryCode={n.home.countryCode} size="sm" />
              <span className="font-semibold">{n.home.name}</span>
            </div>
          ) : (
            <div className="text-text-dim">TBD</div>
          )}
          <div className="border-t border-white/5" />
          {n.away ? (
            <div className="flex items-center gap-2">
              <Flag countryCode={n.away.countryCode} size="sm" />
              <span className="font-semibold">{n.away.name}</span>
            </div>
          ) : (
            <div className="text-text-dim">TBD</div>
          )}
        </motion.div>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: Create `src/components/sections/RoadToFinal.tsx`**

```tsx
import { useMemo } from 'react';
import { Section } from './Section';
import { BracketColumn } from './BracketColumn';
import { useBracket } from '@/data/queries';
import { Skeleton } from '@/components/ui/Skeleton';
import { FallbackBanner } from '@/components/ui/FallbackBanner';
import type { BracketRound } from '@/data/types';

const ORDER: BracketRound[] = ['R16', 'QF', 'SF', 'F'];
const LABEL: Record<BracketRound, string> = {
  R32: 'Round of 32',
  R16: 'Round of 16',
  QF: 'Quarter-Finals',
  SF: 'Semi-Finals',
  F: 'Final',
  '3rd': '3rd Place',
};

export function RoadToFinal() {
  const { data, isLoading, isFallback, refetch } = useBracket();

  const byRound = useMemo(() => {
    const m = new Map<BracketRound, typeof data>();
    (data ?? []).forEach((n) => {
      const arr = m.get(n.round) ?? [];
      arr.push(n);
      m.set(n.round, arr);
    });
    return m;
  }, [data]);

  return (
    <Section id="road-to-final" stage="road-to-final" eyebrow="Bracket" title="Road to the Final">
      {isFallback && <FallbackBanner onRetry={refetch} />}
      {isLoading && <Skeleton className="h-72" />}
      {!isLoading && (
        <div className="flex gap-6 overflow-x-auto pb-4 -mx-4 px-4">
          {ORDER.map((round, i) => (
            <BracketColumn
              key={round}
              label={LABEL[round]}
              nodes={byRound.get(round) ?? []}
              delay={i * 0.2}
            />
          ))}
        </div>
      )}
    </Section>
  );
}
```

- [ ] **Step 3: Mount**

```tsx
import { RoadToFinal } from '@/components/sections/RoadToFinal';
// ...
<LiveMatchCenter />
<RoadToFinal />
```

- [ ] **Step 4: Commit**

```bash
git add src/components/sections/RoadToFinal.tsx src/components/sections/BracketColumn.tsx src/App.tsx
git commit -m "phase 5: RoadToFinal bracket columns with staggered reveal"
```

---

### Task 5.8: FinalCountdown

**Files:**
- Create: `src/components/sections/FinalCountdown.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Create file**

```tsx
import { Section } from './Section';
import { Countdown } from '@/components/ui/Countdown';
import { useFinalMatch } from '@/data/queries';

export function FinalCountdown() {
  const { data } = useFinalMatch();
  const kickoff = data?.kickoff;
  return (
    <Section id="countdown" stage="countdown" eyebrow="The Moment" title="Final Countdown">
      <div className="text-center py-12">
        <div className="text-[10px] uppercase tracking-[0.3em] text-text-dim mb-4">
          {data?.stadium.name}, {data?.stadium.city}
        </div>
        {kickoff && (
          <Countdown
            to={kickoff}
            format="DD:HH:MM:SS"
            className="font-mono text-gold text-5xl md:text-hero block"
          />
        )}
        <div className="mt-4 text-text-dim text-xs uppercase tracking-[0.3em]">
          Days · Hours · Minutes · Seconds
        </div>
      </div>
    </Section>
  );
}
```

- [ ] **Step 2: Mount**

```tsx
import { FinalCountdown } from '@/components/sections/FinalCountdown';
// ...
<RoadToFinal />
<FinalCountdown />
```

- [ ] **Step 3: Commit**

```bash
git add src/components/sections/FinalCountdown.tsx src/App.tsx
git commit -m "phase 5: FinalCountdown section with DD:HH:MM:SS clock"
```

---

### Task 5.9: Phase 5 review gate

- [ ] **Step 1: Type-check + build**

```bash
pnpm exec tsc --noEmit
pnpm build
```

- [ ] **Step 2: Manual scroll through all 7 sections**

`pnpm dev`. Scroll top → bottom. Each section reveals on enter, fixture data visible.

---

## Phase 6 — Scroll-Guide Ball

### Task 6.1: GSAP scroll progress wiring

**Files:**
- Create: `src/hooks/useScrollStage.ts`

- [ ] **Step 1: Create file**

```ts
import { useEffect } from 'react';
import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
import { useUIStore } from '@/store/uiStore';

gsap.registerPlugin(ScrollTrigger);

export function useScrollStage() {
  const setBallStage = useUIStore((s) => s.setBallStage);
  const setScrollProgress = useUIStore((s) => s.setScrollProgress);
  const stage = useUIStore((s) => s.ballStage);

  useEffect(() => {
    const trigger = ScrollTrigger.create({
      trigger: document.body,
      start: 'top top',
      end: 'bottom bottom',
      onUpdate: (self) => {
        setScrollProgress(self.progress);
        if (self.progress > 0.05 && stage === 'idle-card') {
          setBallStage('scroll-guide');
        }
        if (self.progress > 0.95 && stage !== 'parked-countdown') {
          setBallStage('parked-countdown');
        }
        if (self.progress < 0.05 && stage === 'scroll-guide') {
          setBallStage('idle-card');
        }
      },
    });
    return () => {
      trigger.kill();
    };
  }, [stage, setBallStage, setScrollProgress]);
}
```

- [ ] **Step 2: Mount in App**

```tsx
import { useScrollStage } from '@/hooks/useScrollStage';
// inside App component:
useScrollStage();
```

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useScrollStage.ts src/App.tsx
git commit -m "phase 6: ScrollTrigger wires scrollProgress + stage transitions"
```

---

### Task 6.2: Waypoint resolver + curve

**Files:**
- Create: `src/three/useWaypoints.ts`, `src/three/ScrollPath.tsx`

- [ ] **Step 1: Create `src/three/useWaypoints.ts`**

```ts
import { useEffect, useState } from 'react';
import { useThree } from '@react-three/fiber';
import { CatmullRomCurve3, Vector3 } from 'three';

const ORDER = [
  'today-matches',
  'progress',
  'groups',
  'featured-teams',
  'live',
  'road-to-final',
  'countdown',
];

function rectToWorld(
  rect: DOMRect,
  viewport: { width: number; height: number },
  size: { width: number; height: number },
): Vector3 {
  const cx = rect.x + rect.width / 2;
  const cy = rect.y + rect.height / 2;
  const x = ((cx / size.width) * 2 - 1) * (viewport.width / 2);
  const y = -((cy / size.height) * 2 - 1) * (viewport.height / 2);
  return new Vector3(x, y, 0);
}

export function useWaypointsCurve(): CatmullRomCurve3 | null {
  const { viewport, size } = useThree();
  const [curve, setCurve] = useState<CatmullRomCurve3 | null>(null);

  useEffect(() => {
    const compute = () => {
      const points: Vector3[] = [];
      for (const id of ORDER) {
        const el = document.querySelector<HTMLElement>(`[data-ball-stage="${id}"]`);
        if (el) {
          points.push(rectToWorld(el.getBoundingClientRect(), viewport, size));
        }
      }
      if (points.length >= 2) setCurve(new CatmullRomCurve3(points, false, 'catmullrom', 0.4));
    };

    compute();
    const ro = new ResizeObserver(compute);
    ro.observe(document.body);
    window.addEventListener('scroll', compute, { passive: true });
    window.addEventListener('resize', compute);
    return () => {
      ro.disconnect();
      window.removeEventListener('scroll', compute);
      window.removeEventListener('resize', compute);
    };
  }, [viewport, size]);

  return curve;
}
```

- [ ] **Step 2: Extend `useBallController` to sample the curve in `scroll-guide`**

Edit `src/three/useBallController.ts`. Add at top:
```ts
import { Vector3 } from 'three';
import { useWaypointsCurve } from './useWaypoints';
```

Inside the hook (before the switch), add:
```ts
const curve = useWaypointsCurve();
```

Replace the `scroll-guide` case:
```ts
case 'scroll-guide': {
  if (!curve) return { position: cardWorld, rotation: [Math.PI, 0, 0], scale: 1 };
  const p = new Vector3();
  curve.getPointAt(Math.min(1, Math.max(0, scrollProgress)), p);
  return {
    position: [p.x, p.y, 0],
    rotation: [Math.PI, performance.now() / 800, 0],
    scale: 1,
  };
}
```

- [ ] **Step 3: Commit**

```bash
git add src/three/useWaypoints.ts src/three/useBallController.ts
git commit -m "phase 6: waypoint resolver + Catmull-Rom curve sampling"
```

---

### Task 6.3: Mobile / reduced-motion ball sprite

**Files:**
- Create: `src/components/sections/MobileBallSprite.tsx`
- Modify: `src/components/sections/Section.tsx`

- [ ] **Step 1: Create sprite**

```tsx
import { motion } from 'framer-motion';
import { useReducedMotion } from '@/hooks/useReducedMotion';
import { useBreakpoint } from '@/hooks/useBreakpoint';

export function MobileBallSprite() {
  const reduced = useReducedMotion();
  const isDesktop = useBreakpoint('lg');
  if (isDesktop || reduced) return null;
  return (
    <motion.div
      initial={{ x: -60, opacity: 0, rotate: 0 }}
      whileInView={{ x: 0, opacity: 1, rotate: 360 }}
      viewport={{ once: true, amount: 0.5 }}
      transition={{ duration: 0.6, ease: 'easeOut' }}
      className="absolute top-4 right-4 w-6 h-6 rounded-full bg-gradient-to-br from-white to-gold shadow-gold"
      aria-hidden
    />
  );
}
```

- [ ] **Step 2: Mount in Section**

Edit `src/components/sections/Section.tsx`, add:
```tsx
import { MobileBallSprite } from './MobileBallSprite';
```

Change root `<section>` to `relative`:
```tsx
className={cn('relative max-w-container mx-auto px-4 md:px-8 py-16 md:py-24', className)}
```

Add inside the `<section>` before children:
```tsx
<MobileBallSprite />
```

- [ ] **Step 3: Commit**

```bash
git add src/components/sections/MobileBallSprite.tsx src/components/sections/Section.tsx
git commit -m "phase 6: mobile ball sprite per section"
```

---

### Task 6.4: Phase 6 review gate

- [ ] **Step 1: Manual desktop scroll test**

`pnpm dev`. Scroll slowly top → bottom. Ball follows curve through sections. Reverse scroll → ball returns.

- [ ] **Step 2: Mobile (DevTools responsive)**

Switch to iPhone 12 preset. Confirm canvas not mounted (no WebGL warnings), small ball sprite appears per section on scroll.

- [ ] **Step 3: Reduced motion**

OS-level enable `prefers-reduced-motion`. Refresh. No ball, no sprite, sections still fade in.

- [ ] **Step 4: Build**

```bash
pnpm build
```

---

## Phase 7 — Polish + QA

### Task 7.1: Performance — lazy-load Three.js bundle

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Convert GlobalCanvas to lazy import**

```tsx
import { lazy, Suspense } from 'react';
// remove direct GlobalCanvas import
const GlobalCanvas = lazy(() => import('@/three/GlobalCanvas').then((m) => ({ default: m.GlobalCanvas })));
// ...
<Suspense fallback={null}>
  <GlobalCanvas />
</Suspense>
```

Also fix the default export shape — add to `src/three/GlobalCanvas.tsx`:
```tsx
export default GlobalCanvas;
```

(Keep named export too.)

- [ ] **Step 2: Build + analyze**

```bash
pnpm build
```
Confirm a separate chunk for Three.js appears in `dist/assets`.

- [ ] **Step 3: Commit**

```bash
git add src/App.tsx src/three/GlobalCanvas.tsx
git commit -m "phase 7: lazy-load Three.js GlobalCanvas chunk"
```

---

### Task 7.2: SEO — Helmet head tags

**Files:**
- Create: `src/components/seo/HeadTags.tsx`
- Modify: `src/App.tsx`, `public/og-image.png` (placeholder)

- [ ] **Step 1: Add an OG image placeholder**

Place any 1200×630 PNG at `public/og-image.png`. A solid Midnight Pitch color with white "MATCHDAY" wordmark is fine for MVP.

- [ ] **Step 2: Create `src/components/seo/HeadTags.tsx`**

```tsx
import { Helmet } from 'react-helmet-async';

const TITLE = 'Matchday — FIFA World Cup 2026 Companion';
const DESCRIPTION =
  'Live scores, group standings, bracket, and the road to the Final. The premium World Cup 2026 companion.';

export function HeadTags() {
  return (
    <Helmet>
      <title>{TITLE}</title>
      <meta name="description" content={DESCRIPTION} />
      <meta property="og:title" content={TITLE} />
      <meta property="og:description" content={DESCRIPTION} />
      <meta property="og:image" content="/og-image.png" />
      <meta property="og:type" content="website" />
      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:title" content={TITLE} />
      <meta name="twitter:description" content={DESCRIPTION} />
      <meta name="twitter:image" content="/og-image.png" />
      <meta name="theme-color" content="#0A1428" />
    </Helmet>
  );
}
```

- [ ] **Step 3: Mount in App**

```tsx
import { HeadTags } from '@/components/seo/HeadTags';
// inside the fragment, first child:
<HeadTags />
```

- [ ] **Step 4: Commit**

```bash
git add src/components/seo/HeadTags.tsx public/og-image.png src/App.tsx
git commit -m "phase 7: SEO head tags + OG image"
```

---

### Task 7.3: SportsEvent structured data

**Files:**
- Create: `src/components/seo/MatchSchema.tsx`
- Modify: `src/components/sections/TodayMatchesSection.tsx`

- [ ] **Step 1: Create `src/components/seo/MatchSchema.tsx`**

```tsx
import type { Match } from '@/data/types';

export function MatchSchema({ matches }: { matches: Match[] }) {
  const items = matches.map((m) => ({
    '@context': 'https://schema.org',
    '@type': 'SportsEvent',
    name: `${m.home.name} vs ${m.away.name}`,
    startDate: m.kickoff,
    eventStatus:
      m.status === 'live'
        ? 'https://schema.org/EventScheduled'
        : 'https://schema.org/EventScheduled',
    location: {
      '@type': 'Place',
      name: m.stadium.name,
      address: m.stadium.city,
    },
    competitor: [
      { '@type': 'SportsTeam', name: m.home.name },
      { '@type': 'SportsTeam', name: m.away.name },
    ],
  }));
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(items) }}
    />
  );
}
```

- [ ] **Step 2: Mount in `TodayMatchesSection`**

```tsx
import { MatchSchema } from '@/components/seo/MatchSchema';
// inside component after data:
{data && data.length > 0 && <MatchSchema matches={data} />}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/seo/MatchSchema.tsx src/components/sections/TodayMatchesSection.tsx
git commit -m "phase 7: SportsEvent structured data per today match"
```

---

### Task 7.4: Error boundary

**Files:**
- Create: `src/app/ErrorBoundary.tsx`
- Modify: `src/app/providers.tsx`

- [ ] **Step 1: Create file**

```tsx
import { Component, type ReactNode } from 'react';

type State = { error: Error | null };

export class ErrorBoundary extends Component<{ children: ReactNode }, State> {
  state: State = { error: null };

  static getDerivedStateFromError(error: Error): State {
    return { error };
  }

  componentDidCatch(error: Error) {
    console.error('Caught at boundary:', error);
  }

  render() {
    if (this.state.error) {
      return (
        <div className="min-h-screen flex items-center justify-center bg-bg text-text p-8">
          <div className="max-w-md text-center">
            <h1 className="font-display text-4xl mb-3">Something went wrong</h1>
            <p className="text-text-dim mb-6">{this.state.error.message}</p>
            <button
              type="button"
              onClick={() => location.reload()}
              className="px-5 py-2 rounded-full bg-gold text-bg-deep font-semibold"
            >
              Reload
            </button>
          </div>
        </div>
      );
    }
    return this.props.children;
  }
}
```

- [ ] **Step 2: Wrap providers**

In `src/app/providers.tsx`, wrap children:
```tsx
import { ErrorBoundary } from './ErrorBoundary';
// inside Providers:
<HelmetProvider>
  <QueryClientProvider client={queryClient}>
    <ErrorBoundary>{children}</ErrorBoundary>
  </QueryClientProvider>
</HelmetProvider>
```

- [ ] **Step 3: Commit**

```bash
git add src/app/ErrorBoundary.tsx src/app/providers.tsx
git commit -m "phase 7: app-level error boundary with reload action"
```

---

### Task 7.5: Lighthouse + a11y pass

- [ ] **Step 1: Build + preview**

```bash
pnpm build
pnpm preview
```

- [ ] **Step 2: Run Lighthouse in Chrome**

Open `http://localhost:4173`. DevTools → Lighthouse → Mobile + Desktop reports. Record:
- Performance ≥ 70 mobile / 85 desktop.
- Accessibility ≥ 95.
- Best Practices ≥ 90.
- SEO ≥ 90.

- [ ] **Step 3: Fix high-impact items**

Common issues to address inline:
- LCP image missing dims → add explicit width/height where applicable.
- Color contrast on `text-muted` body text → only use `text-muted` for borders.
- Missing `lang` on `<html>` → confirm `index.html` has `<html lang="en">`.

- [ ] **Step 4: Commit any fixes**

```bash
git status
# commit if needed:
git commit -am "phase 7: Lighthouse fixups"
```

---

### Task 7.6: Cross-browser + mobile real-device

- [ ] **Step 1: Desktop browsers**

Manual smoke in latest Chrome, Edge, Firefox, Safari (macOS). Confirm hero intro, scroll ball, all 7 sections render without console errors.

- [ ] **Step 2: Mobile real device**

Open on iPhone Safari + Android Chrome via local network (`pnpm dev --host`). Confirm: hero stacks correctly, no Three.js canvas mounts, sections scroll smoothly, audio plays on tap.

- [ ] **Step 3: Note any browser-specific issues + fix**

```bash
git status
# commit if needed:
git commit -am "phase 7: cross-browser fixups"
```

---

### Task 7.7: README

**Files:**
- Create: `README.md`

- [ ] **Step 1: Write README**

```markdown
# Matchday — FIFA World Cup 2026 Companion

Premium single-page companion experience for the 2026 World Cup. Pitch-themed navbar, persistent Three.js football guide, seven scroll-driven sections, three live data sources with fixture fallback.

## Setup

Requirements: Node 18+, pnpm 8+.

```bash
pnpm install
cp .env.example .env.local
# fill in keys (optional — fixtures used when keys empty)
pnpm dev
```

## Scripts

- `pnpm dev` — Vite dev server.
- `pnpm build` — Production build.
- `pnpm preview` — Serve the production build locally.
- `pnpm exec tsc --noEmit` — Type-check.

## Environment

| Key | Purpose |
| --- | --- |
| `VITE_WC2026_KEY` | World Cup 2026 API key (optional). |
| `VITE_API_FOOTBALL_KEY` | API-Football key (optional). |
| `VITE_BDL_KEY` | BallDontLie FIFA bearer token (optional). |
| `VITE_USE_FIXTURES` | `true` forces fixture data even when keys present. |

If any key is missing or the API fails, the UI falls back to local fixture data with a banner indicating sample data is shown.

## Architecture

See `docs/superpowers/specs/2026-06-03-matchday/` for full planning docs.

## Data sources

- World Cup 2026 API — teams, matches, groups, standings, bracket.
- API-Football (`v3.football.api-sports.io`) — head-to-head, historical.
- BallDontLie FIFA — match events, player stats.

## License

Not affiliated with FIFA. Educational / personal use only.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "phase 7: README with setup, scripts, env, architecture"
```

---

### Task 7.8: Final sanity build

- [ ] **Step 1: Clean install + build**

```bash
rm -rf node_modules dist
pnpm install
pnpm exec tsc --noEmit
pnpm build
```

All must succeed. Confirm `dist/` produced.

- [ ] **Step 2: Preview + walkthrough**

```bash
pnpm preview
```
Open in browser. Walk full page end-to-end. No console errors.

- [ ] **Step 3: Final commit if needed**

```bash
git status
# commit any housekeeping
```

---

## Self-Review Notes

**Spec coverage check:**

- `PROJECT_PLAN.md` MVP scope → Phases 0-7 cover all in-scope items; deferred items (3D trophy, PWA, deep routes, multi-page) explicitly excluded.
- `UI_UX_PLAN.md` 7 sections, hero 3-col layout, navbar pitch concept, footer → Tasks 1.7, 1.8, 3.9, 5.1-5.8.
- `COMPONENT_ARCHITECTURE.md` folder layout + component contracts → followed exactly across tasks.
- `API_INTEGRATION_PLAN.md` three adapters + ApiError + enriched hooks + fixture fallback → Tasks 2.1-2.10.
- `ANIMATION_STORYBOARD.md` intro timeline beats 0-5 → Task 4.5 timeline; section reveals → Task 5.x `whileInView`; scroll-guide → Task 6.x. Section-specific micro animations (progress ring, stat bars, bracket reveal) all wired.
- `DESIGN_SYSTEM.md` tokens + Tailwind extend + fonts + Skeleton/Pill/Card/LiveDot → Tasks 1.1-1.4, 3.4.
- `IMPLEMENTATION_ROADMAP.md` phase-by-phase → mirrors this plan 1:1.

**Type consistency check:**

- `BallStage` literals match between `uiStore.ts` and `useBallController.ts` and `useScrollStage.ts`.
- `Match` shape with `home`/`away` `MatchTeam` consistent across fixtures, types, MatchCard, hero, sections.
- `data-ball-stage` attribute names ('today-matches', 'progress', 'groups', 'featured-teams', 'live', 'road-to-final', 'countdown') match between Section components and `useWaypoints` ORDER array.
- `data-ball-anchor` names ('logo', 'date-card') match between `Logo.tsx`, `TodayMatchesCard.tsx`, and `useBallController.ts`.
- `EnrichedResult<T>` shape consistent across all query hooks and consumer components destructure (`data`, `isLoading`, `isFallback`, `refetch`).

**Known intentional simplifications:**

- WC2026 adapter mappers are stub pass-throughs — Phase 2 includes a discovery sub-task (Task 2.4 note) to flesh out once provider response shape is known.
- `useTournamentProgress` reuses `useFinalMatch` and hardcodes `played = 32` — a dedicated stats hook is post-MVP polish, not a blocker for the section's visual layout.
- Audio anthem MP3 is a placeholder — real anthem file can be swapped without code changes.
- Telstar PBR ball is post-MVP polish — icosphere ships now.
